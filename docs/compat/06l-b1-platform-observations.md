# B1 platform and production-control observations

Root verified isolated host b1cff8a7/client9d090ed source, compiled binaries,
raw output and internal screenshot/snapshot pairs. The reproducible platform
harvest is `evidence/platform-isolated/harvest_b1.py`; its qualification JSON
records exact files, hashes and limits. This is functional evidence, without
a performance or complete rendering-equivalence claim.

Mac native Alcher passes on 274/289 with both CPU pixmap and GPU texture
presentation measured in actual panel counters. All internal PNGs were read.
The GPU adapter is Apple M4 Max/Metal. A separate native Flax274 diagnostic
fails at six flax and supplies the colocated wall/flax snapshot for the host
identity repair. Its rendered scene does not qualify the gameplay loop.

The Mac production panel Pause button holds observed alchs/items/coins stable
after drain; Resume produces fresh work; Stop shows idle and clears script
paint; Start produces new work. The first run ended before Resume/Stop and
is retained as incomplete. CUA actions and observations are in the timestamped
native-controls JSONL. Internal terminal screenshots identify the same session
but do not serve as screenshots of every control action.

Windows native289/old-catalog Alcher consumes one noted Rune chainbody and
Nature rune, adds65MagicXP and30000coins, and writes a read internal scene2
PNG. RTX5060 Laptop/Vulkan is the selected adapter. Client frame-routing
counters were not sampled, so GPU routing remains unqualified there. Two
500ms responsiveness timeouts in the first four seconds are retained among
225 probes. The owned one-off scheduled task was removed and absence verified.

Linux headless289/new-catalog Alcher passes its exact core witness. Concord
runs the Linux-built TUI binary in an actual140x40 PTY with a four-line private
store entry and no product-code overlay. All6242 package files and1213 engine
files were verified. Two TUI core runs pass. Actual terminal mouse input drives
Pause/Resume/Stop: seven notes and runes with600000coins remain stable across
two paused observations; Resume reaches780000coins; Stop shows idle/cleared
paint and810000coins after an in-flight action. Raw terminal bytes, reconstructed
screens, timestamps and forwarded inputs are retained. Start/Browse/settings,
reconnect and full clean-screen acceptance remain separate.

The first TUI run interleaves debug stderr with rendering. Separating stderr
exposes a second defect: a PASS line prints while the alternate screen is still
active. Card t_35ee61ac owns the bounded reporting correction. Both functional
passes remain valid; neither proves a clean display. Concord's owned289 engine
was stopped and the original274 system service remains inactive. No Xvfb or
Concord compilation occurred.

The11 as-used support scripts are retained under
`evidence/platform-isolated/helpers-b1/`, with original paths and hashes.
Their private stores and original root-relative paths are recorded in the
receipts; replay requires those inputs and the explicitly selected controlled
world. Every failed attempt remains alongside the successful observations.
