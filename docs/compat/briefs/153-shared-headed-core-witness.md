# Reuse full catalog proof in the visible panel

Use Sol defaults on codex/rs2b0t-multirevision after reviews139 and144 and audit151. Read06ap-headed-core-and-dragon-diagnosis.md and AGENTS/execution. Operator requires future LIVE to be visible so they can watch prepared actors and gameplay. This is proof wiring, not foreign script or gameplay implementation. Preserve all existing predicates, deadlines, failure receipts and ownership.

Own the extraction of existing Observation/CoreCase/CoreWitness/cycle observers from host-play/tests/catalog_boundary_live.rs into one shared host-play proof module, the integration test consumer, narrow host-play library export/feature wiring, and opt-in headed catalog_watch/panel LiveScript/session proof wiring. Do not edit script runtime, scenario preparations, client, engine or other fixture branches. Root serializes native37 and option147 behind this card; Mule127 paired files can continue. All future solo fixture authors must use shared core module, not restore duplicate observers.

Use the audit recommendation but inspect exact current reviewed source. Reuse ALL existing core predicates rather than reimplementing a subset. Headed catalog_watch must record the same qualified scene2 pre-Start baseline and script-start marker, then feed compact observations from the production slot's existing snapshot publisher. No duplicate world capture, observer deep-copy per frame, JSON roundtrip, bot actions, post-Start seeding or second gameplay driver. Preserve cadence/identity semantics; stale/reset/session/relogin snapshots cannot manufacture progress. Scenario failure remains FAIL; scenario PASS alone cannot emit final core PASS until full witness qualifies. Timeout remains FAIL with full compact witness/evidence. Non-core existing panel watches retain their current behavior. Use an explicit catalog-watch opt-in if needed to avoid changing ordinary panel behavior.

Also address the identified proof-capture synchronous serialization hitch within the narrowly touched capture path: queue immutable owned terminal metadata/handle once and serialize off the gameplay observation thread, using bounded ownership and clean shutdown. Do not spawn unbounded threads or retain world snapshots in a recurring queue. Keep screenshot plus matching snapshot evidence and pass/failure drain semantics. Do not optimize animation loading or claim performance wins. Root will measure/capture the actual effect. If a safe capture change substantially expands scope, report and leave this separate; shared core correctness is the primary goal.

Provide a focused move-diff/normalization comparison demonstrating cycle logic is preserved, meaningful actual shared-observer tests from existing suite, and composed headed-bridge tests that reject premature scenario-only PASS, wrong/stale baseline and timeouts, then accept real full-core evidence. Run affected host-play catalog tests and panel checks/tests with relevant features in exact exports excluding unrelated WIP. Do not count compilation or shared-target tests as functional acceptance. Keep test source identities in evidence. No LIVE (root owns visible runs); no remote or gitlink actions. Commit scoped source and report06ar-shared-headed-core.md/evidence/shared-headed-core, request SAME-card reviewer and stop. Root does new visible gameplay proof and whole-branch review.

Known limitation outside this bounded card: paired full-core needs paired slot orchestration/qualifier. Do not claim paired acceptance from solo observation. Identify reusable seam in handoff for later paired wiring.
