Implement plan step 2 only: reconcile the current published 274 preservation work with the existing 289 client. Plan is ../../docs/superpowers/plans/2026-09-10-rs2b0t-multirevision-finish.md from the client directory. Read ../../AGENTS.md and ../../docs/execution.md once. The host campaign is codex/rs2b0t-multirevision; work only in its vendor/fr-client-rust on codex/bothost-274-289. Root prepares the merge; do not merge, push, update remotes, change branches, or edit the host gitlink. The standalone 289 campaign AGENTS.md is intentionally excluded from integration because it names an obsolete external campaign and instruction paths; the current host policy applies.

Inputs: published client 9b41e6e06b9fd42dc2247fc813a303c3fcb92941, 289 source c18f3a1148e9caee73426e162677328ca64d1a83, common ancestor 4f2048ea10f75b3bb92ff45610b35ba7313b0308. Host docs/compat/architecture-review.md is the completed actual Grok 4.6 pre-implementation review. Read its relevant overlap and preservation findings, not old state/history. Scope: client lifecycle/GPU/cache-path reconciliation, required regression coverage and docs/revision-289/bothost-integration-report.md. No host/frontend/API implementation, 377 import, server edits or live accounts in this task.

Consolidate overlay invalidation around the 274 comparison against the last uploaded RGBA/coverage in GpuBackend::finish. It observes the final composited pixels including sealed main-modal RGB and preserves the held minimap on overlay-only freeze frames. Do not retain a redundant full-scene hash/epoch scan. The architecture reviewer suggested preferring epoch; root resolves that implementation preference in favor of the already-preserved comparison, with the 289 production hint regression as the falsification gate. Remove newly imported private Renderer overlay_epoch/signature helpers if unneeded; preserve diagnostics and every actual overlay writer. Keep 289 GPU_SCENE_TEST_LOCK. Remove unconditional main-modal/main-overlay forced uploads if the retained comparison already observes their changes, so unchanged chrome stays lazy. Do not weaken any existing behavior assertion. It is appropriate to replace tests that only restate an eliminated private epoch mechanism with production output tests; keep blink/move/clear/late-writer and minimap freeze observations.

Compose CLIENT_UNPACK_DIR with operator_home(), retaining explicit HOME (including blank), Windows USERPROFILE fallback and existing local cache semantics. Preserve boxed appearance storage, animation-base Arc sharing and borrowed delay reads, dynamic sprite recycling, resource accounting, Windows socket support, default 274 constructors, packet framing, script tick-relevant publication and all 289 primary-source packet fixtures. Add a focused constructor/Arc-sharing regression across default 274, explicit 274 and explicit 289 if not already covered. No new world copies, bot action API, opcode table, timeouts or scope expansion.

Use CARGO_TARGET_DIR=/Users/acfrazier/experiments/274bot/.worktrees/rs2b0t-multirevision/target/client. Root ran cargo test --locked --workspace on the published 274 base: 774 passed, zero failed/ignored, GPU overlay/freeze/modal tests executed; receipt is ../../docs/compat/evidence/client-baseline-274/. Run client cargo fmt --all -- --check, cargo clippy --locked --workspace --all-targets --no-deps -- -D warnings, cargo test --locked --workspace, and explicitly run the ignored npc_hint_production_gpu_blink_move_clear_cache_and_freeze test with real GPU. Exercise CPU tutorial/presentation tests already in the suite. Capture commands, commit IDs, counts, failures and bounded diagnostic rationale in the integration report and fresh raw receipts under docs/revision-289/evidence/bothost-integration/. Do not skip required GPU tests or claim Linux/Windows execution on macOS. Known historical platform limits are recorded in ../../docs/compat/fixture-inputs.md; none failed in the fresh macOS baseline. Fix scoped regressions; retain failed logs. Do not rerun unchanged failures until lucky.

Commit only the client reconciliation and report/evidence. Hand this SAME card to profile reviewer with kanban_request_review, summary and exact commit/test evidence, then stop. No task model/provider overrides. The per-task Grok 4.5 review must complete before root dispatches the separate required fresh whole-client Grok 4.6 review and integration trial. No release or publication work.

Root prepared merge commit 4098a50fb4803f5d6ec9131af38d18e0ba6a395f. It is deliberately unqualified; GPU reconciliation below is still required. Published 274 baseline receipt is complete. No active build is using target/client. The initial ledger gate is committed at host 081e15ce. Preserve historical 289 reports as evidence; some contain pre-existing intentional Markdown hard-break spaces (git diff --check on all imported docs reports these; product-source diff check passes).
