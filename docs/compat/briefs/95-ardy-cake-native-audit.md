# Diagnose Ardougne core failures and scope native cake-stealing support

Use grok46 defaults. This is a bounded read-only source/live-evidence audit,
not implementation, and can run beside current source owners. Read the frozen
4cc6cc27 source export and new isolated raw logs under evidence/catalog-harness/live/
*r*-ardy-*-4cc6cc27* (six cases, both revisions, old catalog), plus both frozen
catalog sources. Own only docs/compat/04u-ardy-cake-native-audit.md and unique
evidence/ardy-cake-native-audit/; no production, fixture, engine or foreign
edits, no LIVE, no compilation and no cache cleanup. Root commits design files.

Determine the exact first blocking owner for ArdyCakes Flee, ArdyThiever Guard
and Knight. Both revision cases timed out at first Thieving XP without item,
XP or movement. Root saw shim cake_stall.stealCakes choose Locs.query().nearest()
without filtering and return false when the closest object is not the stall;
it also ignores the options argument. cake_stall_data pins all stand/flee
coordinates to2661,3301 while the fixture begins2668,3312. These observations
are not permission to clone the foreign CakeStall loop or assume every failure
has the same cause. Trace TaskBot validate-first execution, Flee and restock,
Game.inCombat/HP, cake call shape and selected loc facts. Establish each cause
with exact paths/lines and bounded evidence.

Audit existing native capabilities and selected server/cache world data for
Baker's stall object ID/name/operations/tiles, legal adjacent stand and flee
locations, guard interaction and thieving success/refusal/combat chat. Identify
which native Rust operation/state-machine boundary should own an authorized
cake-stealing capability and which arguments/results the shim must map. Search
this checkout and its explicitly selected native engine/cache paths only.
Preserve host lifecycle, command ordering, timeouts, route ownership, fallback
semantics and incomplete ancillary-feature behavior. No foreign policy tables,
CakeStall controller copy, task-loop reimplementation in JS, or fabricated
success/refusal counters. Spell missing capabilities as BLOCKED: missing <op>.

Deliver a concrete bounded implementation slice and file ownership compatible
with named-bank -> special -> teleport -> shop -> Make-X -> fire runtime queue.
Distinguish a simple native query/mapping correction from missing gameplay
controller behavior; enumerate supported options/callback and return contract
that must be preserved, and honest unsupported limits for any future-owned
policy. Include meaningful unit/isolate regressions and root's unchanged-window
LIVE falsifier (actual stall steal+XP, deposit, closed-bank return, further;
Guard/Knight actual coin/XP and full bank cycle). Do not dim either card based
on missing host mapping. Do not claim source audit or seeded state as LIVE.

Complete this read-only audit card with actual source identity and report path;
no routine reviewer card is needed for the design. Root evaluates and dispatches
any authorized implementation under exclusive ownership.
