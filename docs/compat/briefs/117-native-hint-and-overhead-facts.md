# Expose existing native hint and local overhead facts

Use sol defaults on codex/rs2b0t-multirevision, after helper115. Read current
AGENTS/execution and design05v sections2-3. This is original campaign work,
not a new foreign game-policy runtime. Own only needed API snapshot fact
projection, script Snapshot schema/isolate_fb/encoder/decoder, client_adapter.js,
focused new tests, and unique report04ah-native-hint-overhead.md / evidence/native-hint-overhead/.
If host-play producer is required, own only fact population there; no lifecycle,
protocol, navigation, or client edits. No fixture/ledger/STATE/LIVE.

Missing reader.selfChat kills both Duel listeners in root04d logs. Native
LocalPlayerView.player.actor.overhead_text already exists from client chat_message.
Post this exact local overhead text (not chat ring's latest message), null when
absent, clear on logout/restart and expiry. Reader.selfChat returns that optional
string unchanged; no countdown synthesis or imported fight state machine.

Missing reader.hintTile blocks Brimhaven chase. Client HINT_ARROW already
stores hint_type/hint_tile_x/hint_tile_z. Post only coordinate hints; return{x,z}
for coordinate type2 (decoder normalizes2-6), otherwise null, respecting
native clearing behavior. Do not invent a target/pillar or retain a foreign
guardian. actions.setRun(on) maps onto the EXISTING set-run isolate op and
Interactions::set_run. Preserve availability and command ordering.

Add only required flatbuffer fields, backward-readable absent defaults,
reused buffers and bounded per-tick allocations; no world deep copy. Meaningful
native-to-isolate tests cover local/other actor distinction, current overhead vs
chat log, clear/logout/no hint/type, two successive snapshots/old absent buffer,
and queued set-run shape. Exact source+overlay, one exclusive target reused
by reviewer, disk check, focused affected tests and strictClippy. Commit scoped
files, SAME-card reviewer, STOP. Root owns native/headless actual Duel and
Brimhaven proof after review. No parser/opcode duplication or foreignpolicy.
