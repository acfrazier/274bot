# Full Wildy and Brimhaven core fixtures

Use sol defaults after fighter109 review. Design05v is the bounded task:
implement full native circuits/progress sections4-6 with selected content loc
destinations verified read-only from maps/source. Own only scenario/src/lib.rs,
host-play/tests/catalog_boundary_live.rs, unique05y-agility-full-core-fixtures.md
and evidence/agility-full-core-fixtures/. No runtime/client/engine/ledger/STATE
or LIVE. Signal117 separately owns hintTile/setRun; do not implement those here.

Wildy must cross ridge, complete allfive obstacles with lap XP bonus, and
next pipe progress (>=598XP from southStart), not ridge-only. Brimhaven must
pay/enter, real movement+XP, firstTag(no ticket), later ticket2996, subsequent
movement/XP. No seeded ticket, postStart teleport, or fake failures. Do not
extend180s/150ticks; if cannot fit gold, explicit failed phase/diagnostics do
not replace full authored core. Existing native policy, Eat, guardian behavior
and unsupported ancillary helpers unchanged. No card dimming for host gaps.

Use exact source+overlay and one exclusive reused compiler target with disk
check. Meaningful ordered-witness false-positive tests as design05v§9;
focused scenario/catalog tests and strictClippy. Commit scoped files and
request SAME-card reviewer, thenSTOP. Root owns eight revision/catalog LIVE
cells after both fixture and signal117 review. Do not duplicate foreignarena
BFS or edit GnomeCourse, resource, combat or bank semantics.

## R1 scope clarification from orchestrator, 2026-09-11

The full implementation request already authorizes the minimal harness support
needed for truthful ordered proof. Also own crates/scenario/src/proof.rs and
runner.rs solely for a new per-step fresh XP gain proof (or equivalent bounded
platform-change proof if actually needed). Preserve existing StatXpGain's
once-per-skill semantics and every existing timeout. Prefer a separate variant
that captures its baseline at step entry and cannot consume earlier hop/ticket
XP; missing baseline fails closed, reset/session/step boundaries clear it.
Use it for post-ticket subsequent work after the first pre-Tag hop+XP arm.
Add meaningful tests rejecting prior XP, unchanged observations and missing
baselines; accept actual later observed gain. No broader proof DSL redesign.
If a platform-change arm is needed before first Tag, its baseline must compare
actual native platform identity, not any within-platform player movement.
The existing independent catalog witness remains required too. The same-card
review must cover these harness semantics and the previously requested report.
This narrow extension supersedes the earlier no-runtime restriction only for
these scenario proof/runner changes; no gameplay/client/engine edits or LIVE.
