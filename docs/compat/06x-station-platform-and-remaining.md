# Station cycles, fresh Windows coverage, and remaining failures

Root checkpoint 2026-09-11 11:39 UTC. Host91289e9b and04d7b1f4,
client9d090ed; exact source exports and verified isolated binaries.
These are selected functional results, not full45-card acceptance.

Station/Ardougne912:23 cells,14 PASS and9 FAIL. Both revisions and both
catalogs pass bronze/steel smelting with ordered withdrawal, production,
bank deposit, fresh restock, closed return and further production. Knight
pickpocketing passes allfour cells; Guard passes289 both catalogs. Guard274
misses XP deadline. Cake steals real items but misses bank-return deadline.
Cook variants stop early; reviewed81606759 maps native cooking location
shape and awaits fresh LIVE. FlaxSpinner reaches Spin/Make-X interactions
but misses first XP; Make-X owner has the logs. Failed family cells were
not blindly repeated on the other catalog. Matrix rows remain PARTIAL.
Machine-readable station-ardy-91289e9b.json and raw logs retained.

Native Mac289 newer steel912 passes; screenshot was actually read:
restocked ores, a further Steel bar at furnace3275,3185, Bank trips2.
Native Windows289 newer steel04d also passes with the same observed
bank return and further production. Its screenshot was actually read.
Windows process exit0, no runtime errors; two early500ms responsiveness
probe timeouts out of225 probes remain recorded. The owned one-shot
scheduled task was removed after completion and absence checked.
No actualGPU-backend performance claim from requested mode/paint.

Windows04d: all7597 packaged regular files verified; three builds
(headless/native/TUI), six focused check groups pass. Reused the completed
exclusive Windows target; it was not empty. Root04d Mac reused a completed
exclusive combat target. The first catalog check failed because the exact
export lacked an immutable-input path; after binding the hash-verified
input directory, only failed/unrun checks were resumed. Both original
failure and passing retry are retained. No exported source was patched.

Remaining04d old-catalog local tests:16 FAIL, eight cases on each revision.
ChaosDruid fails prepared Attack40 before Start; HerbloreSecondaries fails
fresh seed-bank acknowledgement before Start. MossGiant misses
ranged.rangeLoadoutOf; HillGiant misses foodCount; AutoFighter misses
SettingsStore.displayString. GnomeChop sees no Magic-tree XP. CoalTrucks289
sees no mining XP;274 mines but misses truck arrival. FlaxAIO289 misses
full pack;274 reaches deposit but fails180s deadline before closed return.
These are separate failure phases, not broken-catalog conclusions.

Paired Air912 both revisions reaches shared Start then fails Paint.buttons;
Duel04d both revisions receives tick callbacks but throws reader.selfChat
in listeners, then never observes real combat in a pen. No pair PASS.
All20 failures have receipts and log hashes in
remaining-and-paired-04d7b1f4.json; append-only core ledger now319 entries.
Native paint113 owns Air seam. Combat helper design112 owns diagnosis;
root posted the additional selfChat finding. Worker fixture109 is disjoint.

Actual model receipts verified for cook107 Grok4.5 run1319, special36
Grok4.5 run1323, agility110 Grok4.6 run1320 and paired111 Grok4.6 run1321.
Mule pair114 now owns bounded offline fixtures, not trade-runtime changes.
Design05w proposal to create a JS trade driver is not implementation
authorization: any missing host policy must stay in Rust; ancillary foreign
helpers remain explicitly unsupported unless the current plan scopes them.
Root retains original host behavior and deadlines.

Fresh Windows headless Mule289 newer PASS: withdrawal27, craft27 Air/135XP,
deposit27, fresh27 essence restock, closed return, further27 Air/270XP.
Receipt/log hash and ordered witness independently checked. Linux latest already-completed proof was earlier
TUI289 at04:31 Eastern; no fresh04d Linux run is claimed.
