# Native nearest-bank access boundary evidence (t_b3223dcc)

Unique extracts for `docs/compat/04k-native-bank-access-boundary.md`.
Raw LIVE logs and the East/West sequence stay in
`docs/compat/evidence/vial-bank-return-ownership/` and
`docs/compat/evidence/catalog-harness/live/`. Frozen catalog `Bank.ts`
lives under `.superpowers/inputs/` (gitignored). Do not treat these
helpers as product source. No LIVE or product edits.

`05l` remains historical. This directory does not copy its mined logs.
