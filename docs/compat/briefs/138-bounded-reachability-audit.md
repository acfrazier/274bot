# Bounded reachability audit after resource2d589

Work in codex/rs2b0t-multirevision. Audit only; do not edit runtime, fixtures, foreign catalog or client. Read applicable AGENTS/docs/execution and fail-closed-dispatch skill. This is a bounded architecture/fidelity investigation, not permission to clone the foreign reachability engine.

Root live evidence docs/compat/evidence/catalog-harness/live/r289-flax-aio-pick-100adccc-resource2d589.log shows actual 28 flax picks and deposit, then remains at bank (2721,3493,0), bank open. Diagnostic lists many field flax 40+ tiles away as reachable_adj=true. Original FlaxAIO GoToFieldTask validates nearestFlax()==null; nearestFlax asks Reachability.canReach(tile,{adjacentOk:true,maxSteps:400}). Our shim/reachability.js ignores maxSteps and reads unbounded native flood bits. Foreign localReach.ts bounds BFS expansions (not route length), including a default cap. That may cause foreign task selection to stall, rather than missing bank-close or missing flax. Do not change original task policy or inject bank close.

Compare the two frozen catalogs, host native reach ownership and exported bits, exact maxSteps/default semantics needed by enabled cards, and determine minimal Rust-owned capability. Prefer bounded native reusable flood metadata or cached projection, no per-query deep world copies or JS BFS. Identify ownership, lifecycle/reset/delta and memory costs, with targeted proof design including distant reachable target rejected at small budget and nearby accepted, adjacency/collision and region reset. Do not demand bit-for-bit foreign router cloning if unavailable; explicitly state semantic compatibility limits. Establish whether this finding explains observed control flow using existing raw facts; do not claim a fresh live pass.

Write docs/compat/reports/09c-bounded-reachability-audit.md with concrete recommendation and files. You may commit only that report and evidence under docs/compat/evidence/bounded-reachability-audit/. Complete audit card directly with verdict and exact commit (this is independent Grok4.6 audit; no routine child review). No LIVE. Existing workers own load.rs and scenario/test files. Preserve them.
