Correct remaining direct-owner lifecycle fixture fidelity gaps after escalation t_6e97dd77, which is the prerequisite parent. Work /Users/acfrazier/experiments/274bot/.worktrees/t_a1f4796f branch codex/memory-diagnostics; verify branch, follow AGENTS/docs/execution. Profile sol defaults, no delegation. This is corrective implementation for new escalation findings after original t_a6d0e3df completed review, not a duplicate routine review. Read parent final report docs/memory/direct-owner-lifecycle-escalation-review.md when available and root current37be3a1 test.

Root inspected current helper: _direct_preflight_fixtures returns synthetic ambient-start for real helper, and literal release JSON expires300.0 while lifecycle fake_pf separately supplies time.time()+60. Original lifecycle before16fdb97 sampled actual server/helper processes. rmc builds role_identities from pf into later receipt/accounting checks. Restore current actual process identity for both real dummy server/helper in lifecycle fake_pf; do not fake the real collector identity or bypass identity validation. Create coherent generated release/receipt timestamps and finite expiry: actual hashed release JSON and fake_pf expiry must agree. Default synthetic admission-unit fixtures and their deterministic clocks must keep existing behavior; do not change production policy or widen a live release.

Own only docs/memory/test_run_managed_cell.py, docs/memory/direct-owner-lifecycle-fixture-correction-report.md and diagnostics/direct-owner-managed-extension/lifecycle-fidelity-correction/. Address all confirmed parent findings relevant to latest37be3a1; known missing release path/cache roots already fixed, keep those fixes. Preserve scoped preflight/managed-receipt mocks but keep real six-file hash rechecks, launch creation, actual dummy child, collector through observe-end/Stop/C/frontend+launcher exit, cleanup and every lifecycle assertion. No runtime Python/Rust changes, no cap/timing weakening, private/live/native/SSH actions, STATE edits or evidence deletion.

Add a portable focused regression for the COMPLETE shared lifecycle fixture construction, even when actual Linux execution is skipped: validate_spec; build all cache/receipt/release bindings; assert sample process start identities equal fresh actual dummy process samples; assert release-file expiry equals the supplied preflight expiry and is valid on same finite test clock; rehash6files with the real verifier. Share fixture construction with the Linux lifecycle test so this test cannot silently cover a different setup. Do not reimplement a parallel lifecycle/controller or mock away failures. Run affected portable tests; distinguish any baseline Mac timing failures from this correction, with actual baseline proof if claiming preexisting. Native Linux full suite remains root-owned after review; do not call skipped Linux test passing.

Append scoped commit, no amend/reset of root commits, update report with exact new fixes and evidence, hand SAME card to profile reviewer with kanban_request_review then STOP. Reviewer independently verify parent findings fixed and portable shared-construction regression actually exercises Linux fixture inputs, plus all preserved real lifecycle semantics. Root will rerun full native suite before any live release. Previously prepared native03 package37be3a1 is withheld and must not be used as corrected proof.
