# Await asynchronous TaskBot validation

Use Sol defaults in campaign codex/rs2b0t-multirevision. Own ONLY TaskBot prelude in crates/script/src/shim/mod.rs, a NEW isolated integration test file crates/script/tests/task_validation.rs, docs/compat/04av-async-task-validation.md and unique evidence/async-task-validation/. No LIVE, client edits, remotes, STATE, support-matrix, or edits to load.rs/load_isolate.rs (native144 owns their WIP). Root owns source integration. Verify branch before edits. Read applicable execution and fail-closed-dispatch instructions.

Root found exact df2 GreenDragon stall: frozen GreenDragon.ts Traced.validate is async, wraps every task, and returns awaited inner.validate boolean. Current prelude TaskBot.loop tests if(task.validate()) directly so Promise(false) is truthy and always runs ContinueDialog, starving GearEquip/Eat. Both frozen Bot.ts loops await validation; this is compatibility binding correctness, no new foreign policy. Minimal repair must settle validate result before deciding whether to invoke execute, preserve task order/one selected task, await execution and existing Rust lifecycle/cancellation/hold behavior. Do not port foreign Bot implementation or introduce a new JS scheduler/router. Use native host gating as already exposed; if broader native change is essential report it to root first.

Tests must prove async false does not execute or starve later async true, selection waits for pending validation, and rejection is surfaced rather than hidden. Exercise actual isolated runtime ABI, not string-matching source. Existing sync/await-execute tests remain relevant. Add pause/stop pending-validation coverage if current lifecycle allows actual delayed promise; do not let a validation resolving after Stop dispatch gameplay. Keep bounded; no broad runtime redesign. Reproduce at least the async-false starvation test failing before repair, then pass on isolated source. Use exact export of committed HEAD+your scoped patch, excluding concurrent source WIP and retaining current committed client aef. Reuse only your exclusive compiler cache. Same-card reviewer request after scoped commit/report; then stop.

Evidence: docs/compat/evidence/catalog-harness/live/r{274,289}-green-dragon-100adccc-geardf2.log and qualification-geardf2.json. Root holds newer Dragon runs. Source audit151 t_9f72114a can independently confirm mechanism, do not wait to reproduce it. Root handles full source validation and headed gameplay.
