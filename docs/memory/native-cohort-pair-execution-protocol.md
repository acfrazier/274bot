# Native cohort pair execution protocol

Predeclared before either N16 role is launched or read. This specializes
current-latency-companion-plan.md sections4-7 without changing its targets.

Run exactly one reference then one candidate Windows panel N16 active
focused-one pair. Heads e25f32806957b1a44c75a598cbdb7ab53afc5383 and
ca56e14371d1edb8c09df1276638ce196d502e36, with their frozen native clients.
Use the final native build/test receipts and identical reviewed instrumentation.
Both runs use120s warmup600s observe5s finite cohort tail60s teardown grace,
responsiveness/fine/render/GPU-completion/scheduling profiles identically enabled,
system allocator, no owner census, BOT_DEBUG unset and no input-seam tracing.
Preserve the matched native server/cache/catalog/nav/settings/geometry/backend.

The input population is slot0 only; decode includes all16 active slots.
Use exactly one sequence of120 ordinary alternating Left/Right pulses per role
from the unchanged reviewed invoke-panel-input-stimulus.ps1, SHA256
04822c0e9f5ade2d555e408cc707722c234bc1e7b442676975a16e7efcaebbd1.
DurationSeconds120 gives120 pulses at1000ms cadence with80ms holds.

Before launch, bind only the stimulus plan and future artifact locations.
After the frontend launches, root verifies actual PID/start/binary/session,
fresh scene2/capture-enabled/slot0-focus images, and actual rectangle/Game Image
point. Record observed values; never copy a prior run identity or invent geometry.
Begin the single helper sequence60–90 seconds after the recorded observe-start
qualification is published, using monotonic controller time to enforce this
operational start window. Retain that trigger receipt and timestamps; source
cohort monotonic records determine actual [START,END) event membership.
If setup misses the start window, record incomplete input evidence and preserve
the run. Do not extend observation, repeat pulses, or rerun the cell for a pass.

Archive the immutable helper, predeclared plan, fresh binding/captures, helper
PID/start and sampled CPU/RSS, controller/sampler accounting, completed stimulus
receipt, all cohort/sample/qualification/diagnostic files and terminal status.
A completed stimulus receipt is checked after the run, never required before it.
Match its PID/start/binary/window and cadence to this run; SendInput completion
is not host acknowledgement. Failed or missing bindings/accounting are explicit
unavailable evidence, not a performance pass. Include helper resource cost in
paired reporting without counting the same process twice.

Require reviewed controls, native no-launch contract/preflight proof and frozen
canonical metadata before launch. This protocol does not substitute for those
gates. Compare complete raw fixed-window cohorts and resources independently
for both roles, preserving every unavailable reason. No selective sub-window,
post-end admission, endpoint relabeling, or campaign acceptance from this pair.
