# BankSorter capability-boundary evidence

Read-only grok46 audit `t_395bc0f4` / brief 80. Not LIVE. Not a ledger
row. Hashes in `hashes.json`.

Catalog sources compared with `cmp`; all seven BankSorter/sort/junk
files are identical across `100adccc` and `8e7d965b`.

No product files were edited. Concurrent WIP on this checkout was not
used as the tested binary.
