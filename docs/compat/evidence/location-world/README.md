# Isolated location/world qualification in progress

Host 50f2be8a216bf96691d37454c1d70644e9ed835b / client 56d80272bcbda3eb1e22db096c1c5e21d3497de4.
The source and binaries use dedicated empty-target build receipts under
catalog-headed. Four default GnomeCourse cells pass across both catalogs and
revisions. Root verified ordered observed log, ground-return and pipe milestones
with increasing ticks and Agility XP, followed by further next-lap XP. These
are native observations beyond the scenario's first-XP predicate. The stored baseline and selected milestones are on plane 0. Native publication
uses client.minusedlevel, and actual loc dispatches also include levels 1 and 2;
the stored milestones do not capture every upper-floor player transition.

Three failed cells remain: 274 old-catalog DoorOpener throws because settings.tile
returned a raw object without distanceTo; radius-8 old-catalog Gnome on 274 and
289 throws at missing Reachability.walkable after its first lap. Both are
missing host compatibility mappings. The source also re-syncs to the completed
pipe when radius 8 cannot see the log; that separate finding cannot be used to
hide the missing host mappings. DirectNavigator.walkTo is the next unmapped
native operation identified by the design. No card has been dimmed.

isolated-results-50f2be8a.json links all seven completed receipts. The
then-remaining Door/Gate/Flax and radius-8 cells required reviewed mappings.
No performance claim is made from these overlapping diagnostics.

## Tile-qualified follow-up

Settings Tile at 6401d3a4 passed same-card Grok 4.5 review 1209. Root rebuilt
that exact source in an empty target, then ran ten cells. All eight DoorOpener
default-door and named-gate cells pass (both catalogs and revisions): the
selected closed loc ID disappears and the correct open loc ID appears after
Start. `harvest_tile_world.py` independently verifies IDs, locations, attached
scene-2 observations, ticks and hashes; `isolated-results-6401d3a4.json` records
the eight passes and two failures.

FlaxPicker default on the older catalog fails on both revisions: the script
starts, but collects no flax before the existing 150-tick full-pack bound.
The frozen bridge canReach looks only for NPC/ground rows, while the unchanged
script asks for loc coordinates. That is our mapping gap, covered by the active
coordinate-query task. Its full-pack escape branch also requires native
canStep, now separately queued in brief 64. Both batches stopped before the
newer-catalog Flax cells. No script defect or dimming decision is established.

One representative newer-catalog 289 native GPU Gnome run on 6401d3a4 completed
multiple laps and exited cleanly with no runtime errors. Root read elevated
rope/tree scenes and later-lap progress in the native window, and inspected
the persisted terminal PNG showing lap 1 complete, seven obstacles and the
ground scene. Logs include actual level-1 and level-2 loc operations. F12 is
disabled during live scenarios, so the intermediate CUA views are conversation
observations; only the scenario's terminal PNG was saved. This is bounded native
proof, not all frontend/settings/lifecycle acceptance. See
`native-gnome-289-6401d3a4.json` for receipt and screenshot hashes.
