# Map existing native count-dialog facts and answer operation

Use grok46 defaults after hostile observation t_eeea20f0 completes same-card
review. Special attack t_68de6f48 will wait for this review, preserving exclusive
client_adapter ownership. Own only crates/script/src/shim/client_adapter.js,
a NEW unique integration test crates/script/tests/client_adapter_count_dialog.rs,
04o-client-adapter-count-dialog.md and unique evidence/client-adapter-count-dialog/.
No API/host/client/schema/runtime dispatcher, fixtures, ledger or foreign edits.
No LIVE. Use fail-closed-dispatch.

Root060 isolated fourold-catalog Tanner soft/hard274/289 cells now get past
Input.invButton and fail at reader.countDialogOpen. Exact raw logs under
catalog-harness/live/r{274,289}-tanner-bot{,-hard}-100adccc-060e15a0.*. Input posts
selected hide1739 slot0 component5382 operation5 bank_generation3, then the
reader throws. Newer-catalog repeats wait after family failure. This is a
missing shim mapping, not an imported script defect; no dim.

Inspect the full Tanner withdrawXById sequence: Input.invButton -> existing
Execution.delayUntil(reader.countDialogOpen,3000) -> actions.answerCountDialog
(count) -> observed Inventory.used increase4000. Map BOTH missing adapter
members now. The host already posts count_dialog_open and already owns
InteractReq::AnswerCount / answer-count (used by ChatDialog.enterCount and
Shop), including existing dialog validation/clearing semantics. The shim reads
posted native state and queues that existing command with the same supported
integer validation; it must not introduce a second withdraw/count owner,
packet, timeout, dialog search or synthetic completion. Returning false from
countDialogOpen when the native fact is false is real state, not missing-op
success. Missing/malformed data must not report an open dialog. Preserve
Stop/reset/logout and existing Rust final validation for stale/closed work.

Use actual isolate bridge tests for closed/open/closed snapshot changes,
valid count queue, invalid values not dispatching, and no answer when no native
dialog if that is the existing operation contract. Compose the exact foreign
call shape from an Input.invButton result through the posted dialog and count
answer where public test helpers permit. Existing host count-dialog tests are
regression controls; no duplicated client opcodes or invented foreign API body.
Audit remaining adapter calls on this Tanner core path and report any further
missing mapping instead of waiting for the next LIVE to discover it.

Exact Git host/client export with owned overlay and new empty target, focused
integration tests and strict affected Clippy. Check disk first; root owns cache
cleanup. Preserve concurrent files. Commit owned paths, request SAME-card
reviewer with exact identity/evidence, then STOP. Root performs Tanner LIVE.
