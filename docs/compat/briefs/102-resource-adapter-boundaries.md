# Audit the resource adapters exposed by first actual LIVE cells

Use grok46 defaults. Read-only bounded design audit; own ONLY
 docs/compat/04z-resource-adapter-boundaries.md and unique
 docs/compat/evidence/resource-adapter-boundaries/. No implementation, build,
LIVE, fixture changes or cache cleanup. Root commits design evidence.

Exact host28d97f38/client9d090ed isolated root source at
.superpowers/review-exports/catalog-root-28d97f38, frozen catalogs100adccc and
8e7d965b. Six GnomeMagicChopper old-catalog cells (chop/short/long bothrevs)
FAIL immediately on Traversal.preload; two CoalTrucks cells FAIL Tools.bestPickaxe.
Raw docs/compat/evidence/catalog-harness/live/*-28d97f38.log. Root preserves
all failed receipts. No actual resource action accepted. Gnome runs have only
prepared equipment/skill/empty products. Coal partial truck cycle has no Seers
haul acceptance; full core remains required separately.

1. Determine exact Traversal.preload contract: frozen Traversal.ts calls
Navigator.start(), void, unawaited. Inspect that start and host nav preparation
at actual production panel/TUI/host-play Start boundaries. The host eagerly
prepares native nav; no foreign worker/router/world import is authorized. Is a
no-op return actually faithful to already-performed native preparation, or is
a native readiness fact/operation needed? Prove the claim with all start paths
and identify honest unbound/failed-preparation behavior. User permits stubbing
ancillary dependencies that would import foreign whale scripts, but do not
fake required navigation readiness or invent arrival. Do not tighten WalkNear,
change route policy or lengthen existing timeouts. Tanner ownership audit100
is separate and owns no implementation.
2. Map minimal selected-game-data native tool facts/query for actual
bestPickaxe, bestAxe, canWieldTool and AXES consumers in Gnome/Coal core. Existing
tools.js AXES id0 and ignored skill/callback fallback are incomplete. Native
Rust owns game-content/tier decisions; JS is thin callback/argument mapping.
Audit actual selected274/289 engines/cache for use vs wield requirements and
item identity, including steel pick6Mining/5Attack versus axes ordinary-use
behavior. Do not copy a foreign Tools policy table or broad toolkit/acquisition
planner. No assumption that a held tool can be wielded. Preserve caller's
availability predicate and actual callback order/return contracts. Identify
minimum tests and native owned files/seams, including slot/profile isolation.
3. Read the next ordinary Gnome/Coal script calls sufficiently to identify
other presently unmapped REQUIRED operations as a bounded checklist so the
implementation can be coherent. Ancillary death/boat/shop/knife recovery stays
explicit, per user's stubbing policy; no native Gatherer rewrite. Do not infer
foreign breakage/dim from host shim gaps. Distinguish required helpers from
full foreign-world dependencies and incomplete imported call paths.

Current ownership: cake97 owns api/content.rs or unique module+lib and
shim/mod.rs; reader101 owns client_adapter.js; selectedUI94 owns registry/UI;
station92 owns shared fixtures. Runtime queue cake->special->teleport->shop->
Make-X->fire uses shared runtime files; audit recommends a serialized insertion
only if necessary. Root decides dispatch after source/evidence review. Complete
report with exact identity, native contract and bounded implementation proposal,
no routine review hop and no claim of live acceptance.
