# Tanner Trade arrival diagnostics

Root retained the four original Root78 soft/hard failures and the native
failure in 06s. Two additional, separately exported diagnostic binaries add
only immutable scene reads immediately before the existing selected Tanner
Trade dispatch. No actions, target selection, route, radius, timeout, fixture
or foreign source changed. Their runs do not enter the acceptance ledger.

Both exports use host78cc99d0/client9d090ed with one documented host-play
diagnostic overlay and the completed exclusive root28 compiler cache. Source
hashes, patches, binary hashes, successful build/Clippy logs, the initial
compile error and all three raw runs remain in
evidence/tanner-arrival-contract-audit/. The harvester verifies each source,
binary and raw record and writes root-diagnostic-summary.json.

| Run | Outcome | Observed work |
| --- | --- | --- |
| 274, native flood diagnostic | PASS, 112333 ms scenario | Tan 27, deposit 27, withdraw remaining hide, return and tan one more; one intermediate modal retry |
| 274, additional bounded512 probe | FAIL, 180015 ms deadline | One batch tanned and banked; restock/return observed, no further tan; three modal retries |
| 289, additional bounded512 probe | FAIL, 102957 ms scenario | No tanning; two modal retries; actual Tanner679 widget nevertheless observed later |

At every observed Trade dispatch, destination3277,3191 is probeable,
walkable and reachable from the actual player tile. The second diagnostic
also verifies native max_steps512 reachability, matching the foreign arrival
probe's bounded BFS contract. Both bounded runs first dispatch from3274,3188;
later274 dispatches also occur at3275,3188. NPC distance varies from3 to6.
The main modal is absent at dispatch; it can appear after the script's
five-second wait has expired. The positive run is retained rather than
discarded: the script can complete through its existing retries.

The original04v proposal to tighten host arrival to force an indoor stand is
rejected. Corrective04x and these observations support the existing host
arrival contract, including its permitted radius. There is no evidence here
for changing host navigation, NPC dispatch or native timeout behavior.
The failure boundary remains the imported Tanner's timing-sensitive
approach/Trade/modal-wait sequence. This report does not claim an unreachable
destination or universally broken Tanner. Acceptance remains incomplete;
any card dimming must state this limitation and preserve the positive and
negative evidence. No retries were performed to obtain a clean pass.
