# Publish existing native quest status facts for required enabled callers

Active campaign codex/rs2b0t-multirevision. Read AGENTS.md, docs/execution.md and
fail-closed-dispatch. After native observation134, before teleport37; own narrow
api/script/host-play transport/query/tests, no fixtures, client or LIVE.

Finished214 FireGiant274/289 old-catalog runs both FAIL not impl: Quests from
checkPrereqs -> Quests.status('Waterfall Quest'). Existing quests.js already reads
snap().quest_statuses but transport never posts it. GameSnapshot already owns
quest_statuses() -> QuestStatusView {component_id,name,colour}, rebuilt from
native quest-tab text controls. Reuse those facts. Do not invent a quest engine,
quest journal/controller, completion, varp policy or foreign quest table.

Publish an additive delta/reset-safe compact quest-status list, resolving the
native status-colour meaning in Rust against both selected interfaces and the
frozen declared return contract. JS all/status remain thin reads/name lookup;
missing/unrecognized rows stay unknown or explicitly unavailable per contract,
not a fake notStarted/completed value. Preserve exact names/status enum strings;
verify heading/nonquest text filtering if required by the real source/API. Map
small documented display colours only with evidence; no large handwritten table.
Use existing native snapshot generation and clear logout/session/replacement
facts, including old buffer absence and explicit empty list. Avoid bulk polling
roundtrips; publish only when changed, no deep world copy.

Quests.journal and points and quest execution remain unavailable unless a frozen
enabled caller proves required; no speculative completion. This task enables a
truthful prereq read only. Root separately prepares a legitimate quest-started
fixture through existing native controls when required; do not lie about quest
state to make FireGiant proceed.

Focused composed producer/FB/JS tests for both selected colours/statuses,
unknown/absent, later clear, per-slot isolation and duplicate-name behavior as
specified. Verify real snapshot producer, not only hand-fed JS rows. Formatting
and strict affected Clippy, exact committed export with exclusive cache.
Report04ai-native-quest-status.md and evidence/native-quest-status. Commit owned
paths, request same-card reviewer and stop. Root owns live requalification.
