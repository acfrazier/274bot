# Remaining combat, casting, shop, trade and production design

Use configured `grok46` defaults. Design/source audit only for the authorized
plan step 6 and frozen 45 enabled cards per catalog. Read applicable host
instructions, fail-closed-dispatch, support-matrix.json and the active plan's
steps 6–7. Banking and provisioning/recovery already have designs/review tasks;
do not redesign or duplicate those families. Root and other workers own source.

Inspect the actual required enabled card callers and settings in the two
full-hash catalog roots from the ledger, then map combat/casting, hostile-player
facts, shop/trade and production API requirements to committed Rust capability
and thin shim code. Identify only concrete unresolved required operations;
not every not-impl occurrence is in scope. Include 289 BankFletcher cut/string
modes and Superheater fire-staff alternatives, and required production actions
for the remaining enabled cards. Scope includes supported options, not only
imports or default startup. Named quest/clue/gatherer/market-maker rewrites,
dim and import-only cards and full 377 support stay deferred. A normal enabled
resource-gathering loop is not automatically the excluded gatherer rewrite.

Write docs/compat/04-combat-production-design.md with a compact caller/branch /
required Rust operation / current gap / precise observable proof table. Group
implementation into coherent bounded capability families with minimal API
shapes and recommended order. Preserve client/host ownership, command order,
Pause/Stop/reconnect and existing action/runtime deadlines. Identify actual
source evidence for revision-specific content differences; never silently
shrink the enabled set. The ledger is evidence, not a script allowlist; users
choose script/revision suitability and host/client refuse unsupported operations.

Prefer current Rust operations and posted facts. No foreign runtime or planner
clones, no general action framework, no second protocol table, and no expanding
unchanged incomplete features outside required enabled callers. Define composed
script -> wire -> Rust -> posted-result tests and bounded live fixture settings
with post-Start deltas; seeded state, a sent request and startup are not proof.
Report product decisions only if outside the already authorized scope.

Own only the named design report; root commits it. Do not edit product, matrix,
STATE, other docs, client, external fixtures or remote hosts. No live action,
process changes, index/git mutations or more delegation. Concurrent bank source
is unreviewed WIP; label it accordingly and read the final task result later.
Record actual model/provider, complete this design card when delivered.
