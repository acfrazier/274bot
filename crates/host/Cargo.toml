[package]
name = "host"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
api = { path = "../api" }
client = { workspace = true }
libc = "0.2"
num-bigint = "0.4"
vault = { path = "../vault" }

# Dev-only: real queue.on_submitted_work_done smoke (soft-skips without adapter).
[dev-dependencies]
pollster = "0.4"
wgpu = "29"
