# Design faithful selected-data catalog choices before Start

Use grok46 defaults. Read-only follow-up to04p-catalog-parameter-metadata.md.
Own docs/compat/04s-selected-item-parameter-choices.md and unique
 evidence/selected-item-parameter-choices/. No code, fixtures, LIVE, ledger
or frontend mutation. Root owns implementation routing. Task t_7bb8a58a owns
registry/settings_store literal/default/anyOf fixes; do not edit those files.

Root accepts04p diagnosis but does not accept exposing unfiltered FODDER in
source order as faithful ALCH_OPTIONS. Frozen AlcherLogic filters FODDER by
native ITEM_DB and sorts by floor(cost*0.6), then label. Repeated display names
have explicit labels. Choices and ordered settings must be backed by selected
revision facts and preserve supported ordering, not a guessed36-key list.
No cloned foreign UI runtime or arbitrary JS evaluation during Browse.

Trace when JsLibrary is registered versus when the native panel/TUI select
an immutable server/revision profile and game-data source. Existing
resolve_setting_options handles loadouts only. Find the smallest native
metadata seam for BOTH frontends that permits the recognized Alcher key and
label expression to resolve from catalog-declared literals + selected native
items before Start. Maintain cached/shared facts; no per-frame world copies,
no source execution, no process-global mutable catalog schema across profiles.
No selected facts must not publish unsupported options as valid. Preserve
existing File Load scope and avoid a general expression interpreter.

Inspect actual frozen expressions and relevant native APIs; report exact
constructor/caller/file plan and lifecycle. Can a small typed descriptor on
SettingDef carry a catalog-declared candidate list to a native resolver with
selected facts, or is an already-bound library registration overload smaller?
Choose using real call sites and ownership, not an abstract framework. Preserve
full original defaults; describe absent default-key behavior without silently
changing script defaults. Include option labels where they distinguish item
identity; do not conflate cosmetic gp formatting with capability support.

Give a bounded implementation task and meaningful tests: same catalog under
both revisions, absent alias, sorted ties, explicit labels/shared names,
profile switch/rebind and unchanged plain literal/loadout options. Identify
one real UI falsifier (custom choice -> Custom item -> fresh Start). Root
controls native proof and all runtime ownership. Complete read-only design
with exact source identities and actual model/provider; no review handoff or
new implementation inside this card.
