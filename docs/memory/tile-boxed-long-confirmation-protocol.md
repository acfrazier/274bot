# Single longer focused confirmation for private tile boxing

Predeclared before either longer cell. This is the one longer stage allowed by
performance-finish-plan.md section5 and tile-boxed-fields-native-protocol.md.
Approved short-screen analysis: backgrounde415ade and focused0501fed. Common
background RSS is lower in both orders, while focused whole/common direction
reverses during startup decay. The named question is whether focused baseline
and candidate converge to distinct resident/private-commit levels after that decay.

Run exactly baseline then candidate, N16 active focused-one, each30s warmup,
600s observation and60s teardown. Use frozen baseline9268890/clientabb811b/binary
 e2deb1db371366f674c18e39d04f7309480310072ade224ffa1433c84d4559b5 and
candidatefb3589a/clientfd956c9/binary
 a9b581bab780d2868035941d799d416f0ef9ec68d5eb5a4fe0a62fa29670667f.
Preserve existing e7158ba clean controls, original restored four-file native
measurement runtime, fixture/cache/server/session/geometry, actual Intel Vulkan,
render settings and profiling flags. No CPU override, navigation captures,
owner census, debug, allocation counter or stack logging. Verify VM Off/zero RAM
and no native frontend/build before each fresh preflight. New dedicated longer
controls and unique cell IDs must pass native AST and complete no-launch checks
before launch. Do not modify existing short controls or native Rust binaries.

Archive every receipt and raw run. Require exit0, strict native binding, complete
match keys,16/16 actual workload qualification and actual one GPU renderer.
Report whole observation and common harness-elapsed intersection, preserving
per-side endpoints/counts; independently recompute contained per-slot metrics.
Keep RSS, current resident maximum, peak-field, private commit and GPU tracking
separate. CPU uses matched user+system and elapsed deltas.

To investigate stationarity without picking favorable endpoints, also report
all consecutive100s elapsed bins fully contained in the common intersection,
anchored at its lower bound, plus the complete final300s common interval.
Report each bin median/max RSS/private commit and CPU, and the first-to-last bin
change. These are descriptive; do not declare a statistical noise band from a
single pair. If memory still decays substantially or conclusions change with
these predeclared windows, classify unresolved startup decay and investigate
that cause or park the candidate. Do not add another longer/reverse/short retry.

A failure in exit/binding/qualification/conditions stops the stage; preserve it.
A candidate without intended memory improvement cannot be retained on this test.
CPU regression above5%, missing/inconclusive p99 within2ms or input evidence,
and absent overhead calibration remain explicit unresolved gates; longer run
length does not repair missing instrumentation. No final budget, retention or
lifecycle acceptance is granted by this protocol or by a passing workload.
Require independent raw recomputation and per-task Grok review of conclusions.
