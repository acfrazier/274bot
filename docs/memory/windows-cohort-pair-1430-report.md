# Native Windows fixed-window cohort pair — 2026-09-08 1430

Both predeclared N16 focused-one roles completed once, exited0 and qualified all
16 active Thiever slots. Both decode cohorts close completely. Candidate slot0
input closes completely; baseline input is unavailable because root missed the
predeclared60–90s trigger window. No repeat or catch-up pulses were sent.
This is fixed-window endpoint accounting evidence, not campaign acceptance.

| Evidence | Reference e25f328 | Candidate ca56e14 |
| --- | --- | --- |
| Decode population / events | 16 / 16000 | 16 / 16000 |
| Decode losses | 0 | 0 |
| Decode aggregate fine p99 bucket | 24–25ms | 24–25ms |
| Input population / events | unavailable / 0 | slot0 / 120 |
| Input fine p99 bucket | unavailable | 41–42ms |
| Input losses | unavailable population | 0 |
| Main process exit | 0 | 0 |
| Stimulus validation | incomplete: trigger missed | complete |
| Frontend median resident bytes | 662515712 | 687902720 |
| Frontend lifetime peak resident bytes | 1062477824 | 986865664 |
| Frontend sampled CPU cores | 0.5258711 | 0.5261696 |

The CPU/RSS rows are diagnostics from the full observation interval, with no
helper subtraction or accepted savings. This pair has asymmetric physical input
and baseline configuration inspection overlapping observation. Both source/client
roles are frozen separately; the native generic adapter's full match keys differ
at client_sources_sha256 and host_conditions. No generic matched resource or
latency non-regression acceptance is asserted from equal aggregate p99 buckets.
The exact raw resource reader reports missing_resource_provenance; the separate
native receipt adapter independently binds both builds, cache, runtime settings
and continuously sampled game_server/controller/bootstrap/launcher/collector
roles (status available). Instrumentation overhead is explicitly unmeasured.
Availability of those bindings does not promote the raw resource gate to a pass.

Candidate helper15100 was spawned once74.90993900000467 seconds after the
controller publication. The completed controller validation binds its immutable
source, target14872/start identity, rectangle, scene/capture/focus receipts and
120 alternating pulses. Wrapper2460 has121 valid resource samples; input helper
has120 valid samples plus one unavailable final sample after exit. Final sampled
cumulative CPU is0.109375s wrapper and2.46875s helper; last sampled RSS is39120896
and99172352 bytes respectively. These are sampled summaries, not exact lifetime
peak/CPU accounting. Target frontend cost is not counted again as a helper.
Host cohort completion is separate from SendInput success. The endpoint is the
existing panel presentation acknowledgement, not physical display scanout.

All raw bytes are preserved. Root verified baseline60/60 archived files and
candidate64/64 by SHA256 and length. Source/receipt paths:

- diagnostics/windows-cohort-baseline-1430-archive/artifacts.tar.gz
  SHA6583d20fe29c233ecc79db6cbf0e2a185cd3fcd2d2617fd11ec178ddd66996d8.
- diagnostics/windows-cohort-candidate-1430-archive/artifacts.tar.gz
  SHA502d9eb13df31674148956ba9b5c5eeb2532cfe92f848392cdfd286f5019357d.
- diagnostics/windows-cohort-native-reader-1430/artifacts.tar.gz
  SHAc74bdabde25c09f40c2efe037177d034ed5286920f385c8dfd1e7ede38106bed,
  55/55 files verified, including native reader source modules.
- windows-cohort-pair-1430-evidence.json records the concise derived values,
  reader source hashes, per-slot buckets and distinct process identities.
- windows-cohort-{baseline,candidate}-1430-visual-verification.json records
  the root-read captures and operator observations. Operator saw no baseline
  freeze/black image/missing overlays, and saw candidate camera movement while
  the script continued with no issues. These observations are qualitative.

The initial baseline local reader rejected archive directory raw-run-01. Its
sidecar records the original native run-directory basename. A separate
original-run-path/<original-basename> symlink points to the immutable archived
raw directory; no raw file or reader rule changed. The initial failure and
original-name reader outputs are both retained. Candidate uses the same explicit
path binding. Current cohort reader results are under each archive's
metrics-original-path.json. The native installed legacy reference_metrics reader
predates the cohort and focused-one GPU fixes: its separate output has no cohort
gates and reports unavailable GPU. It is retained, not used to replace the current
cohort results. Current local GPU callback diagnostics are available; they do not
establish hardware scanout or final rendering acceptance.

Independent evidence review is pending. The fixed-window companion stops at this
one pair; missing reference input remains unavailable. Do not rerun this cell to
obtain a pass. Final absolute budgets, matched regressions, lifecycle/scaling and
whole-campaign Grok4.6 review remain open under performance-finish-plan.md.
