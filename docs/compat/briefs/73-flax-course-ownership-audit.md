# Diagnose remaining Flax and course failures after native loc refresh

Use profile grok46 defaults. Read-only ownership audit; write only this task's
report `docs/compat/05k-flax-course-ownership.md` and unique evidence directory
`docs/compat/evidence/flax-course-ownership/`. Do not edit client, API, script,
scenario, fixtures, frozen catalogs, game engines, settings, ledger or timeouts.
Do not run LIVE. Root is running live and platform checks; other workers own
runtime and scenario source. This bounded audit is authorized by the operator's
request to keep assigning useful Grok work overnight.

Inspect exact host b1cff8a7fdf6d18a5f7a3c7c23c6fa8a020e173b and explicit client
9d090ed04957e4efc254f073cda97bc5510ca72b. Verified regular-file export:
`.superpowers/review-exports/catalog-root-b1cff8a7`; manifest and build identity:
`docs/compat/evidence/catalog-headed/source-b1cff8a7.json` and
`binary-headless-b1cff8a7.json`. The current gitlink 4b69c0e96 integrates client9d.
Do not mistake concurrent working changes for this tested binary's source.

1. Revision274 old-catalog FlaxPicker again times out on full-pack proof after
only six exact flax1779, while both revision289 catalog cells pass actual
fill/deposit/return/further-pick. Raw logs and receipts are
`docs/compat/evidence/catalog-harness/live/*flax-picker*-b1cff8a7*`.
The 274 script repeatedly selects 2737,3440 and 2738,3441. Before assuming the
foreign script is defective, trace native loc delete/add application, API loc
cache refresh, host publication and JS loc/coordinate reachability semantics.
Client9d now has a separate static_loc_generation and API refreshes player-relative
scalar distance; earlier source reviews pass but did not establish this LIVE
failure was fixed. Selected274 content deletes every picked flax for25ticks;
289 probabilistically deletes. Both selected engine sources and hash identities
are in existing 03-world-capabilities.md/input-audit. Establish exact ownership
and evidence, including whether unrefreshed native collision/posted reachability
or loc lists still explain the selection. No speculative repair.

2. Default GnomeCourse passes XP94 plus second-log destination on both revisions
and both catalogs. Old-catalog radius8 still fails on both revisions after the
first lap, ending by the pipe exit. Raw logs
`*gnome-course*-b1cff8a7*` and historical `*3be68eaf*` preserve the evidence.
The pipe exit at2484,3437 is more than8 tiles from log2474,3435; frozen AgilityBot
resync appears to select the exit pipe. Confirm against current native distance
freshness and exact foreign source (both catalogs), distinguish a broken option
from a bridge defect, and recommend the precise dim/reason scope consistent
with the operator policy. Do not alter host behavior to accommodate a foreign
script. Do not invent revision gating or hide a host capability gap.

Read current STATE imported-script ownership and temporary ancillary-stub rules.
Use fail-closed-dispatch. No full world cloning, JS navigation policy or
foreign-script patching. Do not change enabled status yourself. Deliver source
line/hash pointers and a minimal falsifiable next proof for unresolved causes.
Tests are optional and only useful if they answer the ownership question; if
needed use verified regular Git files and a fresh empty target, not concurrent
shared caches, and avoid archive extraction/deletion. No mirrored tests.
Commit only the owned report/evidence on codex/rs2b0t-multirevision and complete
this design/audit card with exact findings and limits. No routine second review
card is required for a read-only report; root verifies conclusions before any
implementation or acceptance decision. Root owns all integration and LIVE.
