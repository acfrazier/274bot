# Bound remaining noncombat core implementation and proof

Use grok46 defaults, read-only audit on codex/rs2b0t-multirevision. Own report06as-remaining-noncombat-core.md and evidence/remaining-noncombat-core/. No product changes, LIVE, builds, STATE, ledger, remote actions or queue mutation. Root needs actionable finite coverage, not another general campaign review.

Only the currently unqualified base cards AIO Teleport, ShopBuyout, SmithingBot, LeatherCrafter, Firemaker. Inspect both frozen catalogs and exact host APIs/current queued briefs37 teleport,38 shop,39 production dialogs,40 fire. Existing solo core/fixture pipeline and153 shared headed witness will be reused. Identify which native operations are covered by those queued cards and any required-but-unassigned host capability (for example shim BuyoutLogic.buyoutPlan currently throws; is that core or ancillary under the user rule? Verify exact call). Do not solve missing host policy by copying foreign JS helpers/tables. Selected-world facts must be generated from server/cache and consumed as shared Rust metadata. Give concrete bounded native mapping scope where required by enabled core, or exact honest deferred branch boundary.

Provide a finite fixture list for base/default and materially distinct supported branches, with generated item IDs/requirements and real transition oracles as source-verified proposals. Numeric settings alone do not justify infinite tests; justify branch equivalence, and do not omit bank/return/further work, alternate product/control paths or caller-required native failure cases. Existing fixture differences and source hashes must be explicit. Root will validate chosen setup before LIVE; do not label proposals as evidence. Preserve original clocks/no post-Start cheats/no manufactured PASS. No synthetic widget trees or content-ID universality claims.

Exclude BankSorter: existing04l design and pending operator sorting-policy choice own that gap. Exclude already-qualified/corrective resource and combat branches covered139/147-150/153; do not redo145. Existing paired Nature/Mule/FlaxRunner design05w and trade126/fixture127 remain separate; simply list their outstanding headed/pair/default-vs-extra branch seams from that report without re-auditing or implementing them. Explain the five-card fixture dependencies on37-40 so root can queue coherent tasks. No new generic router/DSL/rewrite. Commit scoped report/evidence and complete audit; report actual profile/model.
