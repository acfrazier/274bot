# Tanner count-dialog progress with unresolved Trade/modal failure

Root candidate78cc99d07/client9d090ed contains3364 exact Git blobs and adds the
reviewed count-dialog adapter. Both binaries were built in an initially empty
exclusive target, copied and hashed. Actual Grok4.5 review1280 approved the
artifact. Its claimed independent empty-target execution reused the implementer
target; root's new target supplies fresh execution checks, and the review-gate
receipt states that qualification explicitly.

Affected checks pass:8count-dialog tests, strict count-dialog Clippy,
28catalog tests,93scenario tests and strict catalog Clippy. Initial Clippy
failed from disk-full; retry passed. The catalog identity test then failed
because the exact export lacked its external frozen-input symlink; attaching
the already pinned inputs made it pass. All attempts/log hashes are retained;
no source was edited to make the checks pass. Final checks receipt is
`evidence/catalog-headed/checks-78cc99d0-inputs.json`.

Four headless cells fail: soft and hard leather on274/289 old catalog. The new
adapter actually withdraws27hide1739 plus2000coins995 via existing InvButton
and AnswerCount27. Native navigation reports arrival near the Tanner, and
selected NPC Trade is queued. The script then times out waiting for679 and
retries banking; no leather or coin consumption occurs. Both soft cells
retain an actual679modal with the expected tan-all control and27hides:
274tick115/289tick120, tile3276,3193. Thus the timeout message alone is not
proof that the modal never opened. Task t_59351ef0 audits timing, dispatch and
publication. Core and foreign timeouts remain unchanged; no dim verdict.

Native274/newer soft also fails:85288ms scenario,91.029s process. Root read
actual CUA bank presentation and the final saved PNG:27hides,2000coins,
Trips0/Tanned0 on another Tanner walk. Profiles was opened through actual UI;
last presentation counters pixmap0/texture3521/bind-noop3519/reregister2.
This is visible failure evidence, not tanning acceptance or performance.

`evidence/catalog-harness/harvest_78cc99d0.py` verifies exact source/binary,
raw logs, ordered identity, actual hide maximum, no product and native PNG/
snapshot hashes. Four headless failures append to the historical ledger258;
summary `evidence/catalog-harness/tanner-78cc99d0.json` also includes native.
The newer headless cells remain gated after family failure. No full Tanner
branch is accepted. Helper copies and every check attempt are retained under
catalog-headed; completed compiler targets were cleaned with ownership and
open-reference checks. Disk-full also crashed four unrelated Grok workers;
root preserved their WIP and resumed the same cards after cleanup.
