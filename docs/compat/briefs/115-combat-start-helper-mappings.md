# Three combat startup helper mappings

Use grok46 defaults; ordinary authorized campaign continuation. Parent113 owns load_isolate.rs until its review finishes. Implement design04ae sections2-4 over already-posted generated items, inventory and settings. Own only ranged.js, food.js, settings.js, new tests/combat_start_helpers.rs and the single eat_predicates foodForms assertion in load_isolate.rs; unique report04ag-combat-start-helper-mappings.md/evidence/combat-start-helper-mappings/. No runtime snapshot/mod.rs, nav, imported catalog, fixtures, ledger, STATE or LIVE.

Empty loadout stays empty; no implicit gear selection or foreign tables. Count matching food slots, not summed counts. Identity food form always available, generated cake aliases only; do not invent pizza/pie/chocolate grouping. Existing unknown heal/eatAtHpThreshold behavior stays. Settings displayString/saved stringify posted bag; display fallback only when absent; resolve semantics unchanged. Source design is accepted for thin projections, not new game policy. Three actual failures are retained in root04d logs; further missing members must stay explicit.

Unlike the design-only no-compilation clause, THIS implementation must run focused meaningful behavior tests in design04ae, existing eat_predicates/settings regression and strict script Clippy. Exact source plus owned overlay, one exclusively owned target reused by same-card reviewer; check free disk. Commit only scoped files, request same-card reviewer with actual checked identities and STOP. No selfChat expansion here. Root owns fresh both-revision/catalog LIVE.
