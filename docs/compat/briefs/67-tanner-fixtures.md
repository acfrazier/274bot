# Qualify TannerBot conversion and bank cycles

Use profile grok46 defaults on codex/rs2b0t-multirevision after the vial/potion
fixture review. Read applicable instructions, docs/execution.md,
fail-closed-dispatch, current STATE.md and section 20 of
fixtures/remaining-production.md. No subagents or LIVE launch. Root owns live
qualification, ledger and frontend acceptance.

Implement shared scenario/catalog-boundary fixtures for the actual frozen
TannerBot, both catalogs and both revisions: Soft leather core with buyThread
false, plus Hard leather as the distinct selected widget/output branch. The
periodic thread-buy option remains pending the separately queued shop capability;
record that limitation explicitly. Preserve imported source and all host policy.

Inspect the frozen script/settings plus selected content/interface facts before
choosing setup: 274 Cow hide versus 289 Cowhide display differences must not
lose exact ID 1739; product 1741 versus 1743 and widget 8686 versus 8690 are
source leads to verify, not assumed universal truth. A Tanner conversation is
not a shop. Use only existing Rust operations and the card's own widget choice.
Missing host operation is an explicit BLOCKED finding with caller/seam evidence,
not permission to implement runtime code or alter foreign behavior in this card.

Start a private fresh actor at the selected Al-Kharid bank, empty except any
legitimate pre-Start prerequisites; bank acknowledged hides/coins, never leather.
Require attached scene2, actual player/catalog identity and observed setup before
Start. After Start prove exact hide consumption and matching leather production,
coin cost, a later fresh loaded bank deposit, restock from the initial bank seed,
and a further natural conversion. No XP is awarded; item/bank/widget observations
must carry the result. Preserve original script and scenario deadlines. Do not
extend a deadline to hide an invalid fixture or failure. Post-Start cheats are
forbidden. Capture enough actual state to diagnose the first failed stage.

Own only relevant new scenario registry/cases in crates/scenario/src/lib.rs,
new CoreCase/witness rows in crates/host-play/tests/catalog_boundary_live.rs,
meaningful family witness tests, docs/compat/05j-tanner-fixtures.md and
evidence/tanner-fixtures/. Preserve existing Gnome 3be68eaf, Superheater and
vial/potion hunks. No product/client/shim/frontend/ledger edits. Reject wrong
product, coins-only/queued sends, seeded leather, stale/closed bank and no further
conversion in the witness tests. Verify an exact committed source export plus
owned overlay and pinned client bytes in a NEW EMPTY target; affected scenario
and harness tests, formatting and strict Clippy suffice. Commit only owned paths,
verify the commit scope, request SAME-card profile reviewer, then STOP.
