# Managed pipeline live qualification — 2026-09-07

The fresh N1 cell completed on the reviewed f93ec76 runner. Independent `bind_side` returned `bound`, workload qualified, native boundaries available, all match keys present, and `managed_resources.status=available`. This is functional pipeline qualification, not paired performance acceptance.

Evidence: `diagnostics/managed-pipeline-qualification-20260907T062612Z/` contains the declared spec, preflight identities, quiet check, completed cell receipt, raw process accounting, and `bound-side.json`. Frontend run: `diagnostics/20260907T062620Z_tui_n1_active/`. Frozen candidate binary SHA b94bef2b437b649418dd3285f657ff77838e16d0696ceff8146aa38f37b27340; no source build occurred during this run.

The earlier cell `diagnostics/managed-pipeline-qualification-20260907T061239Z/cells/candidate_n1_pipeline/` remains failed and unchanged: its relative output path resolved under the collector working directory. Grok4.5 approved f93ec76 with 27/27 runner tests. The new cell has a new ID/index and did not silently retry the failed cell.

The 30s warmup / 120s observation / normal 60s teardown ran in the real default TUI PTY. One slot qualified and gained 8 steals. Both launcher and collector exited 0; the frontend exited normally and cleanup recorded no signals or orphan risk. The collector stopped 2.0384s after the native observation end. Full cache fingerprints passed prelaunch and completion checks. Actual native cache, audio request scalars, renderer absence, and ordinal identity were recorded. Physical audio output is not observed.

The independent process reader validated 177 samples spanning the native wall envelope [1788762434.479290, 1788762554.521502]. Six explicit roles had continuous coverage: game server, controller, launcher, collector, gateway, and server supervisor. Each had 120 RSS samples wholly inside that window. CPU uses the enclosing acquisition interval, approximately 121.0044s, with endpoint excess reported; it is not interpolated exact-window CPU. Median RSS: server 1,106,247,680 bytes, controller 27,017,216, launcher 183,074,816, collector 15,646,720, gateway 257,474,560, supervisor 51,560,448. These remain separate from host RSS. Unsupported macOS pressure remains unavailable.

Host diagnostic sampled span was 118.9878s: median RSS 299,859,968 bytes (285.969 MiB), sampled maximum 310,362,112, process high-water mark 310,394,880, CPU 0.029855 cores. Median RSS exceeds the 256 MiB N1 active target. Scheduling recorded 4,300 intervals/cycles over 119,037ms, 36.1232 loops/s and p99 upper bound 30ms: the >=40 loops/s target is missed. Decode had 196 matched selected edges/dispatches, zero selected lost/canceled/dropped, fine p99 upper bound 32ms; one edge before the contained start was excluded, so global coverage is not claimed. Input has no observed probes; GPU profiling is disabled.

The helper accounting pipeline is now proven functional on a real run. Instrumentation perturbation and sampler causal perturbation are not established by these resource series. No overhead result, CPU non-regression, RSS saving, paired eligibility, final target or lifecycle acceptance follows from this single run. Next are the declared N16 OFF/ON/ON/OFF experiment, matched control/candidate comparisons, cadence investigation, input coverage, and the remaining approved final matrix.
