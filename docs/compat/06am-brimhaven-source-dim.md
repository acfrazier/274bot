# Source-specific Brimhaven dimming

The catalog loader dims the audited BrimhavenAgility card/helper pair because
its imported route policy retries the known broken24->23 plank when avoidance
would require a longer route. Source ownership is established by the independent
Grok4.6 audit06ah and its verified actual-model receipt. This metadata change
does not recreate or repair that planner. Original failures remain failures.

The rule requires the BrimhavenAgility registration name, cardSHA256
771daff07bd4b3d6f2826ab1300d4fd66bcbae0f9d7a76e4a2ad07a4d050e859 and
siblingBrimhavenAgilityLogic.ts SHA256
fedf5f8e05fd43642efb0370352e71e5feebf258a784bc503a2e145c33b46d4d.
Registration and refresh populate the existing unloadable reason, which the
pickers / Start refusal already consume. Reason: “dim: known script defect:
retries a broken Brimhaven plank instead of an alternate route”. Inventory
reporting recognizes this existing dim prefix.

Both frozen catalogs show44 enabled/10 dim after the change, with2 import
blocked,3 registry omitted and1 native reserved unchanged. The original fixed
45-card/180-row ledger stays intact: four Brimhaven requirement rows record
DIM_FOREIGN_DEFECT with exact identity and evidence. This is a diagnosed scope
disposition under the operator policy, not four passing live cells. Newer-catalog
live cells remained held; identical source pairs establish the dim identity.
Changed card or helper versions require fresh assessment and do not inherit this
source-specific dim. Their enabled loader status alone is no support claim.

Verification on isolated host4bad86a / clientaef3952d with the two loader/example
overlays:43 existing loader tests passed; catalog_inventory built; both frozen
catalogs dim the exact pair. Two private controls change only the helper or card
source respectively and no longer match the dim. Frozen inputs are unchanged.
Evidence: evidence/brimhaven-source-dim/ contains exact source manifest, commands,
logs, inventories and hash-gate results. Root reused its exclusive compiler cache.
No new live or performance claims, no additional tests that mirror metadata.
