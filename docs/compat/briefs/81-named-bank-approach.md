# Complete named nearest-bank approach with existing host verbs

Use grok46 defaults. Read fail-closed-dispatch, current STATE and the completed
04k-native-bank-access-boundary.md audit. Implement its smallest composition;
root accepts the ownership correction to05l, not an East PASS or dim decision.
This bounded shim task may run independently of Input.invButton and fixtures.

Own only crates/script/src/shim/bank.js, a NEW uniquely named integration test
file crates/script/tests/named_bank_approach.rs, docs/compat/04m-named-bank-approach.md
and unique evidence/named-bank-approach/. Do not edit existing shared test files,
load.rs, shim/mod.rs, host-play, API/client/nav, fixtures, ledgers or foreign code.
If an existing helper cannot be accessed from the new integration test, use the
public testing surface or report the exact limitation before taking another file.

Bank.openNearest currently maps selected name/action to geometric OpenBooth only.
From Chebyshev>1, compose existing WalkNear to the selected loc tile, radius1,
no teleports, then the SAME named OpenBooth identity (id/name/action/x/z/level).
Use the existing nearest-access family60000ms approach bound, named click5000ms
fresh generation wait and already-open5000ms waitReady. Preserve low-level
openBooth/Banking.open click-only semantics and the existing supplied-stand path.
Do not port foreign bankStand, six-attempt retry, dialog/action fallback or router.
Do not add opcode/API/client methods, coordinate exceptions, altered low-level
distances/timeouts, a new bank session, hold token, or Pause clock.

Choose the named scene loc once using current supported selection. After walking,
require current selected identity/name/action/plane to remain valid and adjacent;
no nearest replacement. Native dispatch remains the final stale-target check.
Wait completion is actual open/loaded/fresh bank generation, never a queued click.
On timeout/missing/replaced target return honest false. Preserve existing
Stop/reset/logout abort, guardian hold and Pause/Resume dispatch semantics.
Non-default unsupported access helper branches remain explicit unsupported.

Meaningful tests use the real isolate bridge to demonstrate distant WalkNear
then same named OpenBooth, adjacent click only, missing/replaced target sending
no click after approach, unchanged unnamed Banking.open click-only behavior,
and fresh-generation completion. Run existing low-level geometric bank tests
as regression controls. Exact Git-blob export and explicit client bytes, new
empty target, affected checks and strict affected Clippy. Commit owned files,
request SAME-card reviewer with exact source identity, then STOP. No worker LIVE.

Root will run East and West vial fill/deposit/restock/return/further-output
proofs on both revisions after review and an isolated build. East still failing
with correct approach remains a host/nav diagnosis; no imported-card dim is
justified by the withdrawn05l verdict.
