# Finite local diagnostic comparison (declared before timing)

Task t_b03522bc. No qualification, remote or production input.
Baseline: native source at 8b4d6f5 (verify identical at current HEAD), release offline locked Mac build. Freeze source manifest and copied fixture/child executables before timing. Candidate is at most one representation correction: primitive last-event record, and only extend to peak/mark records if the isolated last-event comparison is insufficient (requires a new declaration, not repeated timing until green). Leave checkpoint scanning and every guard unchanged for this experiment.

Use existing qualify.generated for (1850048, False), (1850048, True), (1000, 'diversity'). Each stream <=2,000,000 bytes. Three serial fresh-child repetitions per case per baseline/candidate, in that order, with existing owned() and limits CPU30/wall60. outputs=True includes phase3 lossless output. No qualification driver main or threshold gate. Retain every full result, phase measurements, fixture/executable/source hashes and process observations. Stop on unexpected failure, no rerun. Compare every semantic field and output with reference and between roles (exclude only documented identity/resource/path fields). CPU medians and total three-phase CPU, no profiler-derived capacity. At least 10% phase2 and phase3 CPU reduction on both churn cases with exact semantics supports retention; this is a diagnostic criterion, not production readiness.

Ranked hypotheses: (1) per-event JSON construction/destruction plus charged allocator is dominant in interpreted churn; replacing only last_event with fixed scalars predicts >10% lower interpreted CPU while raw remains similar. (2) checkpoint scan explains phase3 alone; no phase2 prediction, so leave unchanged to isolate (1). (3) numeric parsing/hash/guard dominate instead; primitive last_event gives little improvement, reject correction rather than weakening checks. Diversity case bounds a different output-heavy shape, not production populations. No claimed RSS savings from allocation reductions. OS background activity is uncontrolled; retain process observations and disclose other work.
