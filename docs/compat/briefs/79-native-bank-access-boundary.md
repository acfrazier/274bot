# Resolve the native nearest-bank access boundary

Use grok46 defaults. This is bounded architecture/fidelity review, not code or
LIVE. Own only docs/compat/04k-native-bank-access-boundary.md and unique
evidence/native-bank-access-boundary/. Read current STATE, fail-closed-dispatch,
05l-vial-bank-return-ownership.md and its raw evidence. No other mutations.

Root does NOT accept05l's imported-script defect verdict yet. The mechanical
trace is useful: VialFiller East WalkNear radius3 arrives3010,3352, then
OpenBooth repeatedly walks toward3010,3353 and never opens. West passes. However,
the frozen foreign Bank.ts470-510 openNearest first interacts, then explicitly
computes bankStand via reachability, walks to a counter stand and retries an
adjacent booth. It offers higher-level access than the current thin mapping to
open_named_booth_at's geometric-adjacent walk. A difference in mapped helper
capability does not prove the imported VialFiller policy is broken. Read that
exact source and the imported caller before assigning ownership. Do not clone
its JS retry/router body or change existing low-level host interaction behavior.

Determine the smallest native capability/composition required by ordinary
Bank.openNearest / named booth access in the authorized45-card campaign. Check
existing Rust bank stand selection, packed navigation, reachable approach,
ScriptWalkArm, banking/provisioning operations and session/hold/Pause/Stop tokens.
Can the bridge invoke an existing complete Rust bank-access path while retaining
selected name/action/ID and revision/profile identity? If a new bounded native
operation is necessary, specify its ownership and exact completion/refusal
contract. Preserve low-level open_booth_at/open_named_booth_at semantics and
timeouts; do not add coordinate exceptions, foreign routing policy or world
copies. An incomplete host mapping remains BLOCKED, not a foreign dim decision.

The result must distinguish (a) caller bug, (b) intentional low-level host
contract, (c) missing high-level capability, and (d) a current bridge that chose
the wrong existing host operation. Lack of a regression in low-level OpenBooth
does not by itself settle those choices. The default West success is a useful
control, not proof that all East responsibilities belong to the foreign card.

Use frozenB1 and2f source/receipts to diagnose, then inspect current committed
host APIs for an existing path. Shared request/dispatch source is owned by
t_c35281b3, then t_af045121 and other named capability tasks; do not edit it.
Recommend a concrete bounded implementation brief, meaningful negative tests
and root LIVE falsifier if needed. Include original operation/order/timeouts,
selected-bank identity, door/route ownership, stale source/target/generation,
Stop/reset/hold/Pause and observed bank-open/loaded completion. No invented
success, broad navigation rewrite, foreign import, or new public-release scope.
Retain05l as historical evidence and state any correction explicitly.
