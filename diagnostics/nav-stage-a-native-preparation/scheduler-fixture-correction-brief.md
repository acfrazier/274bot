Correct generated scheduler matrix fixture cost after native Concord qualification failure. Work in /Users/acfrazier/experiments/274bot/.worktrees/t_a1f4796f on codex/memory-diagnostics; verify branch and follow AGENTS/docs/execution. Use profile luna defaults, no delegation or model/provider overrides. Scope only docs/memory/nav-tiled-stage-a/test_sharded.py, test_sharded_guards.py if needed, a new docs/memory/nav-scheduler-fixture-correction-report.md, and diagnostics/nav-stage-a-native-preparation/scheduler-fixture-correction/. No production Python/Rust changes, no STATE, no SSH/native/private inputs/accounts/cache/Concord/server/binaries/source bindings/limits/timeout changes.

Read existing test_sharded.py matrix_fixture_doubles and both full schedule tests, actual sharded.py bind_entries/read_ref/bind context, and relevant guard tests. The current reviewed source is a44a930. Concord's fresh clean/counting/integration passed but scheduler guard subprocess exited -9 at302.848932s under existing300CPU/360wall/512MiB/4GiB guards. Last output: full coordinate matrix passed; legacy full_generated_protocol in progress. Preserve failure as failure; no qualifying retry under unchanged source. Root archived5770files as7c500748 in coordinate-concord-scheduler-failure-01.tar.gz, verified all bytes. Root builder cProfile of just legacy full matrix completed67.747s under separate90CPU/100wall diagnostic limits:198,078,546 calls;bind_entries63.282s;2,849,698 frozen_support.admit calls48.851s dominated repeated lstat/path handling/sha. profile-summary.txt and profile.receipt.json adjacent are root diagnostic evidence, not native acceptance.

Implement the smallest test-fixture-only optimization for redundant repeated durability/integrity scans in the two full arithmetic matrix tests. Existing matrix_fixture_doubles already caches read_ref and substitutes writer/storage work; use that explicit seam. Preserve all826 legacy launches and all826 coordinate launches, full ordinal/ABBA order,708 final acceptance slots,830 coordinate cumulative children, budget/deadline/identity/aggregation assertions and actual scheduler state machine. Do not weaken production binding or globally mock admission. Preserve focused real admission/mutation/TOCTOU tests outside the fixture double and add meaningful proof that the new double is confined to the generated matrix and restores the real function afterwards. If caching fixture admission, key it explicitly to full bounded identity rather than a blanket always-success; real-check first observation and independently real-check every final retained file/binding with patch removed, so every output/source binding in the generated matrix is actually validated. Add negative proof that post-cache output/record mutation is caught by this final real verification. Avoid copying scheduler logic or pretending generated immutable fixture caches protect live mutations. No changed tests may remove checks, fake test results, raise resource caps, split qualification to increase total allowance, reduce rows/sweeps/lanes, skip tests or bypass native AS proof. If coverage-preserving fixture-only correction is impossible, report the concrete gap instead of broadening scope.

Run full affected generated suites under existing QUAL_LIMITS, retain raw commands/logs/receipts and source hashes, demonstrate double restored/final real audit exercised and negative mutation rejected. Compare structural counts/calls, not claim production speed or RSS improvements. Root handles fresh native reproduction after same-card independent review. Commit only owned paths/report then kanban_request_review reviewer=reviewer on this SAME card and STOP. Reviewer must verify all matrix assertions preserved, real final audit covers every generated binding, mutation negatives fail as required, and production/tools/limits bytes untouched. No new routine review card.
