[package]
name = "host-play"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
api = { path = "../api" }
client = { workspace = true }
host = { path = "../host" }
libc = "0.2"
vault = { path = "../vault" }
