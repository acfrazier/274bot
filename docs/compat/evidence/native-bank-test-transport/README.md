# Adjacent bank access fixture transport

The old regression directly assigned the JavaScript snapshot. Native bank
sequencing now reads the decoded SnapshotReader projection, so that assignment
cannot furnish native observations. The retained before log reproduces its
failure on committed host4bad86a plus the unrelated loader metadata overlay.

The existing test moves into named_bank_approach.rs to use its SnapshotInput
helper. It still requires exactly one OpenBooth and therefore no navigation
request from the adjacent position. No product behavior changes. Both the gold
stub suite and native bank approach suite pass in an isolated committed-source
export with only the two test files overlaid. Commands and source overlay hashes
are retained. The root compiler cache was reused exclusively; no performance
claim. This fixture correction is separate from quest transport137.
