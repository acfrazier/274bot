# Architecture fan-out: execution decision

2026-09-07. Operator authorized independent architecture fan-out while retaining
measured acceptance. Preserve supported scripts, client fence, game fidelity and
inherited20ms logical progression per behavior-contract.md. No target is relaxed.

Reviewed independent reports: A b5f3b92 (t_1effd619), B fa6b883 (t_63d63bc7),
C64dde78 (t_44acaa83), all actual Grok4.5 review completed. The earlier Composer
publication lane t_97e3926f produced no artifact after two protocol exits; root
reclaimed and parked it rather than repeat the same stalled work. Its absence
does not cancel the independent reviewed designs.

## Decision

Proceed with small discriminating work on the largest defensible owners before
broad migration. The architectural freedom is real; fidelity is an outcome
constraint, not permission to cut simulation or supported observation fields.

| Lane | Next work | What it can establish | Limitation |
|---|---|---|---|
| A: observations | Deterministic rebuild-edge exact dedup prototype with real builders and all six mutation counterexamples | Equal bodies share safely without changing owner gates/epochs; concrete unique allocation removal | ~41MiB optimistic historical N16 TUI allocation ceiling, not RSS; CPU equality cost and real overlap unknown |
| B: transport | Queue consumed-buffer reuse behind current failure-attribution review and A/C discriminators | Fewer output allocations with unchanged FIFO bytes and consume lifetime | May retain more free capacity; not a retained-memory budget solution alone |
| C: presentation | Implement GPU-only GameView lazy CPU upload ownership with exact CPU/GPU transition regressions | Remove never-used upload allocations per actual GPU-only view | ~1.468MiB CPU and separately nominal GPU per view; not additive RSS |
| C: scene | Bounded target-layout/occupancy census using existing client scene fixtures before scene-store changes | Whether compact stamps/tile split actually removes material allocation in relevant modes | Required tile/stamp payload remains; broader representation migration not yet justified |

Do not implement a canonical latest mutable world, cross-client mutable sharing,
variable client timestep, reduced cadence, renderer multiplexing or a compatibility
runtime rewrite. Stronger host normalization and private transport formats remain
options if these discriminators establish benefit and explicit replacement oracles.

## Budget closure is unproven

Native Linux diagnostic N1/N16 RSS167.14/610.68MiB implies ~98.68MiB excess at
N16, but reducing the observed29.57MiB increment to16MiB needs ~203.55MiB less
across N+15. Fixed-cost reductions cannot substitute for that incremental gate.
Archived allocation families overlap and predate fixes; do not add optimistic
A/B/C envelopes or subtract Mac allocation bytes from native Linux RSS.

Native Windows Intel focused-one N16 run225220 completed and qualified16/16:
~942.32MiB current median RSS, .5720core and48.76client iterations/slot/s.
This is profiled diagnostic evidence, no final budget acceptance or paired savings.
Background-mode cell started22:59:59; result pending at this decision. Office
service restart22:58:01 occurred AFTER first observation end (~22:56:04.659),
recorded/restored before second cell. No repeated run is scheduled to erase it.

Review each implementation before live use, freeze exact role source/binary
lineage, preserve failures, and measure allocation removal separately from RSS,
CPU and latency. The approved final matrix/lifecycle/target/scaling and whole-branch
Grok4.6 requirements remain unfinished. No architectural design is itself a win.
