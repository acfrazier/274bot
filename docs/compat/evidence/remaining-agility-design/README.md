Read-only grok46 design `t_33794ec0` / brief 110. Not LIVE. Not a ledger
or STATE change. Report: `docs/compat/05v-remaining-agility-design.md`.

Wildy full core is ridge + five-obstacle circuit + further pipe, not
ridge-only. Brimhaven full core is hop + obstacle XP + first Tag +
ticket 2996 + subsequent work, not fee-only. Gold 180s/150 ticks
unchanged. Missing `reader.hintTile` and `actions.setRun` are host
gaps, not imported-script defects. DeathRecovery / Shop.sell / boat
helpers stay stubs.
