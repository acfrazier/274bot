# Native paint-button checks

Unique extracts for `docs/compat/04af-native-paint-buttons.md`.
Isolated Git-blob export plus owned overlay; exclusive target compile.
Not LIVE.

- ScriptPaint `{id,label}` + host-owned `generation`
- `IsolateCmd::PaintClick` one-shot, actor/generation scoped
- Panel ImGui buttons and TUI digit/Enter controls
- Client for isolated export: `9d090ed04957e4efc254f073cda97bc5510ca72b`
