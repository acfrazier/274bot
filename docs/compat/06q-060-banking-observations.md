# Named-bank approach qualification and Tanner follow-up

Reviewed060e15a0/client9d090ed contains Input.invButton and the shim's named
Bank.openNearest approach correction. Root exported3082 exact Git blobs,
built headless and native binaries in one new isolated target, and passed
catalog24/scenario91 plus strict affected catalog Clippy. The preserved
same-card reviews are actual Grok4.5 runs1261 and1264.

All eight VialFiller cells passed: Falador East and West, revisions274/289,
and both frozen catalogs. Each starts without produced water vials, fills28,
deposits28 into a fresh loaded bank, withdraws28 empties, closes the bank,
returns to the fountain and fills a fresh vial. The ledger retains all twelve
060 cells, including failures, and now has240 rows. Recompute raw hashes,
source/binary provenance and witnesses with catalog-harness/harvest_060e15a0.py.
This qualifies the selected named-bank mapping while preserving low-level
OpenBooth semantics and the older failed East receipts.

Tanner soft/hard on both revisions reaches exact Input.invButton hide1739,
slot0, component5382, operation5 in bank generation3, then fails after Start
on missing reader.countDialogOpen. Newer-catalog variants wait after the first
failure per family. Task t_ee0d3dbd maps the reader and answerCountDialog to
already-owned native dialog facts/commands. This is a missing adapter mapping,
not an imported-script defect; no card is dimmed and no failure is erased.

Native macOS289/newer East VialFiller also passed the shared scenario at
128023ms and stayed open through its180-second soak (process184.911s, exit0,
no runtime errors). The captured panel was read: scene2 at2950,3380, Trips1,
Filled28, one fresh water vial227 and27empties229. GPU was requested with
BOT_CPU absent and the Apple M4 Max Metal adapter logged. Presentation counters
were not enabled, so this run alone does not qualify texture-vs-pixmap routing.
The saved PNG/JSON and immutable log/timeline are hashed in
catalog-headed/summary-native-east-vial-060e15a0.json. No CUA interaction proof
is claimed for this run. Full options/frontends and final integration remain
separate. These elapsed times are not performance measurements.
