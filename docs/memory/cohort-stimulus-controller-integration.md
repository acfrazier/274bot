# Cohort stimulus/controller integration

This is an execution prerequisite, not latency or performance acceptance.

Sampler7427b9b was approved by actual Grok4.5/xai-oauth session
20260908_101945_01e0c8 after root native evidence invalidated the earlier
c875105 approval. Source was integrated through d1f3bf2. The native inert
probe of7427b9b used real Windows Popen and windows_process_sample, with an
explicitly injected inert helper hash; it performed no UI/input/frontend work.
All eight mechanics checks passed. Raw evidence:
diagnostics/windows-cohort-stimulus-root-probes/wrapper-inert-7427b9b.json.
Native retained directory: C:/Users/Austen/274bot-tools/cohort-wrapper-inert-7427b9b.
Wrapper source SHA888ce381580be7d40c626cd4e570aa06ec0d91ce7d20e1052fc2fd8835a38cbf.

Integration changes:

- The existing managed loop first-read time for validated observe-start is
  recorded atomically in observe-start-publication.json. A bounded subclass
  preserves the underlying QualificationBoundaries consumption/errors and is
  restored after the run. It adds no process or independent polling loop;
  its CPU/RSS belongs to the already sampled managed controller.
- The wrapper records that full publication receipt and actual pre-Popen
  monotonic timestamp. It rechecks the60..90 window after writing launch
  artifacts. Nonfinite publication clocks fail before spawn.
- The controller accepts finite fractional delays without rounding and binds
  their difference to the exact publication record. It verifies target identity
  against the managed cell report, helper identity from native creation FILETIME,
  separate helper/wrapper roles, positive RSS and both finite CPU counters,
  raw sample linkage/latest summary, archive file hashes and all120 ordered
  successful external helper pulse receipts. These external events are not
  host acknowledgements; cohort reader acceptance remains separate.
- helperStartUtc may be null because the real sampler supplies creation FILETIME
  rather than UTC text. No timestamp is invented. CPU summaries retain the last
  available cumulative sample; exited-final sampling remains explicitly
  unavailable. They are not falsely labeled exact exit CPU totals.

Validation: controller10 tests PASS including actual producer-to-consumer adapter
execution without schema translation; sampler16 tests PASS; py_compile and diff
check PASS. An initial integration-test assertion used the wrong argv index and
was corrected; the producer was not changed to fit it. Native inert7427 proof
predates the additive publication binding and final pre-spawn recheck; final
source must be staged and verified before actual input. Previousa522 no-launch
proof remains historical evidence only for those source hashes.

Remaining before live: Grok4.6 combined source review; final native inert wrapper
and no-launch contract/preflight; fresh per-cell root scene2/capture/slot0 identity
and screenshot binding; then the one predeclared reference/candidate pair.
No earlier reviewer approval, inert fixture or simulated pulse permits a claim
of actual input coverage, host acknowledgement, p99 or campaign completion.
