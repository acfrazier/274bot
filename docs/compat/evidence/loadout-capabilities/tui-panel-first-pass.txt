tui loadout tests passed earlier in this implementer run, before concurrent
crates/scenario WIP broke the shared compile:

  cargo test -p tui loadout
  6 passed (loadouts popup, rename, slot/qty text, invalid name, combo)

  cargo test -p panel loadout
  5 passed (combo, rename, invalid name, failed save, copy equipment)

The later files tui-loadout.txt and panel-loadout.txt record the blocked rerun.
