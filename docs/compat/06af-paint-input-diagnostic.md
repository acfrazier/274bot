# Native and TUI script paint input diagnostic

Private cd49e147/aef3952d fixture starts the unchanged frozen NatureCrafter
Runner through the ordinary frontends. Two exported-only files select the
script/settings and isolate TUI stores. All4410 source files and each binary
were hash-verified. Original Alcher watchdog remains unmodified and expires;
these runs never count as a gameplay or paired-trade pass.

Native274 bundled app: real CUA click changed Go bank to Resume; the second
click restored Go bank. Each script callback logged exactly once in order,
and both labels/logs were visually read. Two earlier command-line attempts
were incomplete; their failures remain. Host Pause/Stop was not completed
before the watchdog. No native source correction was necessary for the clicks.

Real289 TUI140x40: title/status text fills the six-row paint pane and clips
Go bank. Digit1 reached the script once, but the control was not visible;
this frontend is not accepted. Root also found an above-button saturating
hit-test offset and wrap mismatch. TUI-only130 owns correction and rendered
regressions; root owns a fresh PTY check after review.

The private rune setting was Air instead of canonical Air runes, so foreign
configuration falls back to Nature defaults while displaying Air. This does
not alter the shared Runner paint callback, but excludes route/trade claims.
The next private UI export must use the canonical value. No operator stores
or vaults were changed. Keep private-vault and unpack files out of Git.

See evidence/paint-input-ui-cd49/summary.json, private-ui.patch and real
terminal/input receipts. All original watchdog failures remain FAIL.


## Corrected UI fixture and reviewed TUI rerun (14:29 UTC)

Exact025e31281/clientaef3952d plus the same two private UI-only files now uses
canonical Air runes;4515 source hashes and native/TUI binaries were verified.
Native274 real CUA clicks delivered one Go bank and one Resume callback,
paused/resumed the host script, ignored a paint click while paused, and cleared
paint at Stop. Root visually read each state. An already-issued navigation
route continued; these observations do not claim route cancellation.

TUI289 first manual attempt reached visible Go bank/callback and host pause;
the original watch ended before a complete second callback/Stop proof. That
failure is retained. The controls2 rerun used a real140x40 PTY, read actual
rendered label coordinates, and completed Go bank/Resume, host Pause/Resume,
Stop/paint cleanup, above-button rejection and paused-button rejection in26.6s.
It preserved the original Alcher watchdog, which later failed as expected for
this private UI fixture. Separate controls-result.json says UI PASS and
explicitly denies gameplay acceptance. No NatureCrafter route/trade acceptance.

TUI130 review was actual Grok4.5 run1359 (7 API calls). Source025 includes only
that reviewed TUI fix beyond the reviewed runtime dependencies. Raw frame,
input and callback receipts are in evidence/paint-input-ui-025. Private vault
and unpack directories are excluded from commits. The previous cd49 clipped
button failure and noncanonical setting limitation remain intact.
