# Visible qualification and first-cast hitch

2026-09-11 17:32 UTC. The operator requires remaining LIVE qualification to
run in visible headed clients. Exact df2/aef native mage watches completed on
274 and289 with clean exit0. Root read both captures: revision branding,
Staff of fire, selected Fire Strike and active script at scene2 are visible.
Raw records and scope: evidence/catalog-headed/headed-geardf2-summary.json.

The native scenario ends its predicate at initial Magic XP; it is not the
full catalog CoreWitness used by the four retained headless mage passes.
Audit151 is defining sound reuse of that witness in the visible route.
No more headless cells are launched. Original failures remain in the457-row
ledger, including both df2 GreenDragon failures; newer Dragon cells are held.

GreenDragon source wraps every task in async Traced.validate. The host shim
TaskBot.loop tests the returned Promise without settling it. A Promise(false)
is truthy, selecting the first ContinueDialog and starving later tasks. Root
identified this source mismatch and queued bounded correction152 t_620a63ab,
with a failing-before real-isolate regression and same-card review required.
Audit151 independently checks the diagnosis. No foreign card is dimmed.

The operator observed a hitch on the first Fire Strike. Both watches logged
an observation-callback hitch (274:72.126ms,289:74.563ms) immediately after
the terminal scenario capture request. The shot sink serializes the full
GameSnapshot synchronously inside the observation callback. Output snapshots
are about4.2MB. This is evidence of a likely capture contribution, not isolated
causal timing and not a confirmed animation-loading defect. There is no
performance-improvement claim. See evidence/first-fire-strike-hitch/timing.json.
