Review the complete reconciled client candidate for plan step 2 and the first integration checkpoint trial in docs/execution.md. This is a whole-client review, distinct from the preceding same-card Grok 4.5 implementation review and the eventual whole-campaign release handoff. Read current host AGENTS.md and docs/execution.md once, the plan architecture/step-2 contract, and the frozen manifest supplied by root. Do not use dated client campaign state as instructions. Work read-only except your uniquely assigned report path. Do not merge, push, change branches, edit product code, or inspect the other trial reviewer's report/findings before submitting yours.

Compare client published base 9b41e6e06b9fd42dc2247fc813a303c3fcb92941 to the exact frozen candidate. The source 289 line c18f3a1148e9caee73426e162677328ca64d1a83 shares ancestor 4f2048ea10f75b3bb92ff45610b35ba7313b0308. Review the complete combined behavior, not just the reconciliation follow-up diff. Preserve current 274 behavior and optimization work while admitting source-grounded 289 construction/decoding/outbound/presentation. Root owns Git hygiene; the obsolete standalone 289 AGENTS.md was deliberately excluded from the merge. Existing 289 reports remain historical evidence.

Review the two ownership boundaries: client owns protocol/world/render/lifecycle, Rust host owns bot policy/services. No host crate or foreign runtime dependency may be introduced in client. The host does not yet expose 289 sessions: immutable profile binding and direct host writer adaptation are steps 3 and 4. Full 377 qualification, tutorial/audio parity, mixed fleets and public-live acceptance are outside this milestone. Do not approve any of those by implication.

Check overlay blink/motion/removal/late writers, lazy unchanged chrome, sealed-modal RGB, last-FBO freeze and held minimap, CPU tutorial/chat rendering and diagnostics opt-ins. Root selected the existing exact comparison against the last uploaded RGBA/coverage instead of retaining the added private epoch hash. This preserves the minimap distinction between an overlay-only update and a full chrome redraw, and avoids redundant scanning. Validate its behavior rather than requiring an internal epoch mechanism. Check the coupled production-hint/frozen-minimap case explicitly against source and current test receipts.

Also check appearance packet boxing throughout the new actor decoder and tests; animation-base Arc sharing/borrowed delays; sprite recycling; GPU resource accounting; Windows socket and explicit HOME/USERPROFILE handling; CLIENT_UNPACK_DIR override; default and explicit 274 plus explicit 289 constructors; shared cache/interface Arc identity; framed packet reads and generation publication; revision-specific ordered payloads/lengths; session adoption and reconnect reset. No invented tick opcode, new deep World copies or relaxed timeouts.

Use the frozen tests and raw receipts to recompute result counts and verify which GPU checks actually ran. Old standalone 289 review is not candidate approval. Fresh macOS published-base result is 774 passed, zero failed/ignored. Current Linux/Windows execution is unavailable in this session; historical GPU shade and CRC timing failures must be marked historical, not freshly reproduced. Source/client regression approval may proceed with those platform and later host/live gates explicitly outstanding. Raise newly introduced defects separately from existing limitations.

Report actionable priority findings with exact candidate paths/lines and source/evidence. Do not implement fixes. State inspected base/head, actual model/provider/session, start/end times, tool/security restrictions or retries as timing confounders, tests independently inspected, verdict and scope limits. Return your independent result before seeing the other trial's findings. Root will reconcile confirmed findings, overlap, disagreements and corrections; wall time alone is not a model ranking.

Also inspect the small host fixture correction named in the frozen manifest:
`crates/host-play/src/lib.rs::mint_live_names` and its extended existing test.
The first host dependency check exposed deterministic PID/serial truncation
collisions; the red/green evidence and unchanged-base comparison are in host
`docs/compat/client-integration-milestone.md`. Review the scoped correction's
name-length, invocation isolation and frontend/harness effects. This does not
expand the review to pending profile, navigation or catalog implementation.
The passing API/host/host-play check includes memory-profile support: 441 passed,
zero failed, seven live tests ignored. Those ignored tests are later live gates.
