# Mule core and frontend parameter proofs

Root9cc6adb1/client9d090ed completed all four revision/catalog Mule solo Air
cells. The hash-checking harvest retained ordered withdrawal, temple entry,
crafting and XP, portal exit, deposit and empty pack, restock, closed bank,
return and further crafting. The ledger increases from272 to276 records;
the corresponding matrix rows remain PARTIAL because paired trade and other
supported settings still require proof. Earlier failures remain recorded.
Native289/newer catalog also completed this cycle in135678ms, including
54 crafted/270XP across two trips. See the native summary and original raw
artifacts under evidence/catalog-headed/. GPU was requested; no presentation
counter or performance claim is made.

The TUI parameter proof uses exact9cc source plus a private IsolatedEnv entry
point. The second real-PTY run selected Rune chainbody, saved and reopened the
checked option, and started fresh script work: notes and runes26 to25, coins
30000 to60000. Distinct battlestaff and hide labels were read from terminal
captures. The first run expired before its later input requests were forwarded;
it does not prove persistence. Both runs and input-forwarding records remain
under evidence/catalog-params-9cc6adb1/.

The original native gold fixture continually injects its fixed item setting,
so its three earlier successful alchemy runs cannot qualify edited parameters.
A separate private9ccui export seeds the same initial setting, leaves gold
execution unchanged, then stops the script and clears injection after PASS.
The exported two-file patch, source and binary identities document this scope;
production sources were not modified for this private proof. Its first run was
interrupted operationally by the disk cleanup and supplies no full UI proof.

The second private native274 run completed gold, then actual CUA selected Air
battlestaff alongside Rune chainbody, reopened both checked options, and saved
their canonical source keys. A fresh Start from idle produced20 casts, with
notes/runes6 and profit600k at58s. Preferences reopened during that run still
showed both checked options. The run later logged the bank's missing Air
battlestaff and continued with remaining Rune chainbody stock; it does not
prove Air battlestaff conversion. Process exit0 at184.908s, no runtime errors.
Actual screenshots, saved settings and observation record are in
evidence/catalog-params-9cc6adb1/native274-params2/; complete raw process records
are evidence/catalog-headed/r274-alcher-8e7d965b-9ccui-params2.*.

These parameter proofs do not expand catalog acceptance or assert all settings
and frontends are complete. Timing is diagnostic, not a performance result.
