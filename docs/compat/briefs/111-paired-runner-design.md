# Design remaining paired runner coverage

Use grok46 defaults. Read-only design; own only
 docs/compat/05w-paired-runner-design.md and unique evidence/paired-runner-design/.
No code/builds/LIVE/engine/STATE/ledger edits. Commit scoped report, complete
this design card and stop. Do not create implementation cards yourself.

Audit both frozen FlaxRunner, MuleCrafter and NatureCrafter supported modes and
options against host trade/bank/inventory/navigation APIs. Current paired harness
and brief103 establish fresh bank acknowledgement, observed relog, exact partner
identity, shared Start and180s gold clock. Root912Air289 reached both-started then
failed missing Paint.buttons (design108 separately owns that seam). Do not
redesign it or mislabel missing support as broken script. Solo Mule Air has
complete four-cell core evidence; it does not qualify trade modes.

Propose minimum coherent fixtures for the remaining paired work: actual producer
and runner roles, script-caused offer and two-stage acceptance, exact counterpart
and item/currency conservation, completed delivery/conversion, bank/deposit/
restock/closed return and a further exchange. Follow real scripts' native routes
and skills; do not count first trade or seeded inventory as full. No fake partner
script or hidden harness controller that performs the catalog's work. Harness
may prepare actors before shared Start only. No post-Start loot/coins/teleports
or unsupported player aliases. Respect host ownership, guardian/lifecycle,
unchanged timeouts, generation and memory constraints.

Identify which supported settings belong to equivalence groups and which need
separate native/TUI controls or genuine extra gameplay. User allows ancillary
helpers to remain stubs; don't revive deferred foreign runtime/guardians. Missing
operations, broken imported assumptions and fixture gaps must be distinguished.
Specify exact files/tests and practical sequencing with existing paired harness,
Paint design108, tick106, shop/Make-X work. Do not propose a broad trade rewrite.
