# Direct-owner native generated qualification and release build

Status: root evidence package awaiting independent review. No live capture or
performance acceptance. The original failed qualification remains unchanged.

The frozen production derivative is archive SHA256
`2c36d254c37c5ed56a55f78728357296d62be602f8653ed9ec041cd37d099d5d`,
1124 source members, original host c0709aba and client3456edc8 plus the reviewed
owner instrumentation. It is not the historical executable or moving campaign
source. Evidence identities are bound by
`diagnostics/direct-owner-native-preparation/root-combined-native-evidence-manifest.json`.

## Initial result and corrections

The original Linux run completed 12 of 15 checks successfully: feature graph,
dev build, API owner unit/integration, host owner, script fingerprint/Stop,
host-play owner, TUI compile, generated observer, protocol validator and managed
cleanup checks. Two client checks failed because the exact client lock dependency
was absent offline. The TUI suite stalled in a pre-existing preparation fixture
which created a synthetic slot worker. Root preserved the failure and terminated
only its identified cargo process group after 308.714 seconds. The original
qualification correctly remains false.

Root fetched the exact locked client dependencies without updating locks, then
repeated the two original client commands on Linux: three World owner tests and
one Packet integration test passed (16.519s and3.702s). Generated Python bytecode
was preserved outside the source tree with relocation hashes; all1124 source
members and all locks were checked again. No production source was repaired.
The separate correction receipt leaves full qualification false until the TUI
check is accounted for.

The test-only TUI overlay at25af151 was independently approved by actual
Grok4.5/xai session20260909_140618_7af3eb. Root verified the reviewed helper and
patch bytes before and after transfer, then ran the helper on the Linux builder
in fresh `root-tui-test-overlay-01`, timeout900s, with a clean safe environment,
runtime capture off and synthetic fixtures only. The full helper finished in
74.175s. Its exact offline/locked command with
`memory-profile-no-alloc,memory-owner-capture` passed92 tests,0failed,0ignored,
0filtered. Both preparation fixture tests executed. All three manifest lockfiles
matched after staging and after tests. No matching owned test executable/process
remained in root's subsequent /proc checks.

This overlay changes only the separately hash-bound cfg(test) guard corresponding
to c0709aba..f9675b66 in TUI bin.rs. The staged test source differs from the
production archive and is retained separately. Neither archive nor production
binary is replaced by this test tree. The helper receipt intentionally continues
to say linux_qualified=false because it does not itself establish the combined
native qualification; the raw log establishes this actual Linux test execution.

## Optimized production diagnostic binary

Root separately built the ORIGINAL unmodified production derivative using
`cargo build --offline --locked --release --target x86_64-unknown-linux-gnu -p tui
--bin tui-play --features memory-profile-no-alloc,memory-owner-capture`.
It completed in134.926s. Full source and lock identities were verified both before
and after. Compiler flags/wrapper overrides were absent. ELF/readelf/ldd checks
passed. The immutable copied binary is95595384 bytes, SHA256
`8e400e2d622af2c00280bfb91c741753e0da3df1736bb7d6fe7d0f8251394194`.
Its native path is recorded in root-release-build-01/result.json; no Concord
installation or live launch occurred.

## Evidence and limits

Original failure archive SHA256
`06059fae5610cf32f705648e27224d29aba776a7015204e95277e32e0f811977`
contains24 files. Client correction/release receipts archive SHA256
`70eda2037c2ae073797f0e392fb8797515f7e8e422c74db302f6d39e18e12346`
contains18 files, including preserved bytecode. TUI correction archive SHA256
`a82eccc84c6a978b948c924994dda10ac95df7526bf86c94fcf7d78b3decdedf`
contains3 files. Root copied and verified these archives and their extracted
membership; all four client-fetch/test/release logs matched their receipts.
The TUI log is21881 bytes, SHA256
`801a28843c4d170bc9c812c105374078e507afd1f634282e3a7287996db02843`.

The combined evidence supplies passing counterparts for all15 original generated
qualification steps, with an explicit test-only source exception for TUI. It
requires independent review before root native admission. This is generated
functional/observer evidence, not memory savings or live-workload qualification.
No operator/live vault, account, cache or server was used; unit tests can create
synthetic temporary vaults. The managed-controller timing/source/feature/resource
extension and private target preflight are still prerequisites for the one live
capture. No automatic retry, resource-limit waiver or campaign completion is
claimed.
