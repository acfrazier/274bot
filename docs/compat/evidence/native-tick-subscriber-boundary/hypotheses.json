{
  "preserve": {
    "native_owns_tick_timing": true,
    "snapshot_before_tick": true,
    "pause_skips_tick": true,
    "hold_paint_only": true,
    "parked_pump_no_loop_reentry": true,
    "no_foreign_attach": true,
    "no_tick_end_opcode": true,
    "no_frame_poll": true,
    "no_synthetic_skipped_ticks": true,
    "tickCount_already_posted_snap_tick": true
  },
  "accepted_mapping": "JS Set on BotHost.addTickListener; fire after snapshot+tick from __rs_tick (before onStart) and __rs2b0t_pump (before settle); empty Set no-op",
  "hypotheses": [
    {
      "id": 1,
      "claim": "native has no observed-tick edge; need foreign attach/packet listener/tick-end opcode",
      "status": "falsified",
      "falsifier": "host-play script_observe posts snapshot then SlotScript::on_game_tick on PLAYER_INFO; load.rs serializes Snapshot before IsolateCmd::Tick; __rs_tick / __rs2b0t_pump already consume that edge"
    },
    {
      "id": 2,
      "claim": "BotHost.tickCount is already the subscriber Duel needs",
      "status": "falsified",
      "falsifier": "tickCount reads snap().tick. Duel onStart calls addTickListener. proxy throws not impl: BotHost.addTickListener. Root7c bothrev paired logs FAIL that exact string."
    },
    {
      "id": 3,
      "claim": "reuse this.on / inst._subs chat.message seam",
      "status": "rejected",
      "falsifier": "chat.message is instance IPC for snapshot chat_text deltas. Tick listeners are host-level PLAYER_INFO post-process. Duel calls BotHost.addTickListener."
    },
    {
      "id": 4,
      "claim": "fire only from non-parked __rs_tick",
      "status": "rejected",
      "falsifier": "FightOpponent parks on Execution.delayUntilTicks. observeFightState must still advance fightSignal 3/2/1/FIGHT! and pen leave (duels++) while loop is parked."
    },
    {
      "id": 5,
      "claim": "fire during hold/pause to resemble foreign packet listeners",
      "status": "rejected_policy",
      "falsifier": "Brief: host hold/pause refusal and order must not be weakened. Pause skips Tick. Hold is paint-only. Native remains owner of tick timing."
    },
    {
      "id": 6,
      "claim": "thin JS Set plus fire from existing __rs_tick and __rs2b0t_pump",
      "status": "accepted",
      "note": "Callback glue in JS. Scheduling/transport stays Rust. First-tick fire before onStart so Duel's immediate observeFightState is not doubled."
    }
  ],
  "unsupported": [
    "addFrameListener",
    "addDrawListener",
    "attach",
    "onFrame",
    "onDraw",
    "tickMeanMs",
    "setPacketListener",
    "loc snapshot invalidation",
    "producer pump",
    "COMPAT_RUNNER onStop/onPause/onResume (existing gap, out of scope)"
  ],
  "implementation_falsifiers": [
    "pause or hold delivers listener callbacks",
    "parked ticks do not deliver listener callbacks",
    "start tick double-fires after Duel observeFightState()",
    "listener throw becomes last_error via tick-prefixed log",
    "new IsolateCmd, packet attach, frame timer, or tick-end opcode",
    "addFrameListener or addDrawListener implemented",
    "loop/paint/chat.message order changes for empty Set beyond one no-op call",
    "client_adapter.js, registry/UI, or paired-test sources edited"
  ]
}
