# Run the existing complete paired witness through the visible panel

Use Sol defaults after153,127,155 and156 reviews. Read AGENTS/execution,
05w-paired-runner-design.md, reviewed paired harness/fixtures and153's shared
headed witness report. Root owns all actual LIVE runs. The operator requires
every future live actor to be visible during preparation and gameplay.

Own the bounded extraction/reuse of the existing paired observer and shared
Start barrier into host-play shared proof code, existing paired test consumer,
opt-in panel pair-watch orchestration/example, and required pair fixture
extensions. Preserve127's reviewed Mule bootstrap. Do not invent a second
gameplay engine or have a hidden partner perform the trade. Each participant
must run the actual corresponding frozen catalog script with exact counterpart
identity. Reuse panel slots and their existing snapshot publishers; no repeated
world capture or JSON roundtrip. Keep ordinary panel behavior intact.

Provide the three full pair cells from05w: NatureCrafter Air Master/Runner,
MuleCrafter Air Crafter/Mule, and FlaxRunner Runner/Spinner. Reuse existing
preparations and assertions; add only genuinely missing Flax setup/witness.
Both actors reach ingame scene2 and acknowledge safe preparation before one
shared Start. Existing180s prep/gold and150watchtick contracts remain. No
post-Start seeding, teleports, forced inventory changes, harness trade clicks
or scripted partner. Both actors stay visible and distinct in native UI.

Require exact bilateral offers/confirmation and inventory conservation, actual
craft/conversion XP and products, bank/deposit/restock/closed return, then a
second script-driven exchange/work cycle. A seeded first load, first transfer,
two running isolates or ordinary scenario PASS cannot qualify the full pair.
False-positive tests must cover mixed/stale sessions, wrong counterpart,
one-sided completion, first-transfer-only and missing second production.

Retain real failure receipts, compact per-actor observations, shared Start and
terminal timing/source identity. Integrate153's bounded capture drain without
adding unbounded threads or recurring snapshot ownership. Existing paired tests
must consume the same production witness; show move/normalization preservation
and meaningful composed bridge tests. Missing native operations remain explicit
and go to root; do not implement foreign runtime policy in fixture code.

Keep Nature island/shop/stayInAltar and Mule bankFill=false outside these three
minimum cells, explicitly listing remaining distinct extras for root; do not
claim them qualified. No runtime/client/STATE/ledger/remotes/LIVE. Check exact
exports using an exclusive reusable cache, affected paired/host-play/panel
tests and strict Clippy. Report06at-headed-paired-core.md with evidence/headed-
paired-core, exact identities and limits. Commit, request SAME-card reviewer,
then stop. Root inspects the visible actors and runs the actual pair proofs.
