# Restore native ownership of new shim sequencing

Use sol defaults after native hint117 same-card review. Read AGENTS, execution,
fail-closed-dispatch and audit09. The operator asked how strictly the no-JS-API
reimplementation contract held. Root accepts the audit evidence but does not
accept earlier design exceptions as grounds to call JS gameplay sequencing thin.
This is a bounded ownership correction, not new foreign feature implementation.

On codex/rs2b0t-multirevision, move the introduced bank-open and cake sequencing,
and the expanded autocast arm sequence, into Rust-owned begin/next/result state.
Reuse existing native controllers, facts, transport and lifecycle infrastructure.
JavaScript may normalize names/shapes, invoke user callbacks, dispatch a Rust
next command and await a posted outcome. It must not decide next gameplay phase,
choose timeouts, duplicate cake restock/target predicates, or drive its own retry
policy. No foreign TS body, router, recovery planner or runtime copying.

Scope: Bank.openBooth(stand), named openNearest and openNearestWorld; bounded
stealCakes one-attempt ABI; Autocast.arm. Retain exact selected identity and
fresh container generation, same packets and successful order, false/error
contracts and callback timing. Reuse api/cake_stall predicates. Keep cake ABI
stocked/combat/aborted/no-progress, call onSteal only on observed new food, and
leave classifySteal blocked. No additional steal attempts, stand swaps, loot,
chat classification or restock loop. Autocast missing controls still throws,
missing staff/failed arm still returns false. Do not alter inherited unrelated
helpers or PeriodicBank/DeathRecovery's valid Rust-next/callback split.

Native Pause freezes deadlines; Stop/logout/restart cancel outstanding tokens
and prevent late actions/results. Preserve existing successful timing and native
bounds. AuditF1's newly introduced 120s wait outlives the existing native 60s
approach bound: use the native outcome/bound rather than importing 120s. Preserve
5000ms bank readiness, 3000ms autocast phase and 2400ms single-steal resolution
bounds unless actual existing native owner provides a stricter established bound.
Do not increase clocks/timeouts or claim queued commands as completed operations.

Own narrowly required api/script/host-play code and meaningful tests, report
09a-native-shim-sequencing.md and evidence/native-shim-sequencing/. Other runtime
capability cards must wait for this card's review; root controls dependency edges.
No scenario/catalog fixtures, client/engine, ledger/STATE, remotes or LIVE.
Inspect current branch first. Build from an exact isolated export with one
exclusive named cache; retain source identity and logs, no WIP replacement.

Tests must prove actual multi-phase composition and native result delivery,
missing facts, identity loss, stale generation/token, timeout, Pause, Stop and
restart—not merely JS string mirrors. Run affected api/script/host-play tests
and strict Clippy, then commit scoped changes and request SAME-card reviewer.
Stop at handoff. Root owns targeted bank/cake/autocast live requalification
before subsequent integration; this card does not block foundation stage1.
