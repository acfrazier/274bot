# Check Tanner arrival ownership without changing valid host navigation

Use grok46 defaults. Bounded read-only corrective design audit of04v; own only
 docs/compat/04x-tanner-arrival-contract-audit.md and unique
 evidence/tanner-arrival-contract-audit/. No source/fixture/foreign/runtime/nav
edits, no LIVE/compiler/cache cleanup. Root commits design evidence.

Root DOES NOT accept04v's conclusion that host WalkNear must force indoor
adjacency merely to fit a foreign5000ms wait. The user explicitly requires
preserving valid host behavior/timeouts and dimming diagnosed broken foreign
cards instead of adapting host policy to them. A distance3 result for a radius3
request can be valid. A fixed NPC modal timeout does not itself establish a
host guarantee. Keep04v raw finding that real679 was observed in both soft
cells; re-evaluate ownership separately from the failure symptom.

Read exact78 export, actual frozen Npc.ts and src/bot/input/Input.ts
(interactNpc returns actions.menuAction immediately), TannerBot.walkTo
(skips solely at Chebyshev<=4; otherwise walkResilient radius3), actual foreign
Traversal.walkResilient and event/webwalk/geometry/arrival.ts plus Reachability.
Foreign isArrived uses reachability/probeability after geometric radius, while
our traversal.js currently completes on Chebyshev plus host native WalkNear.
Do not copy that foreign router or policy. Compare the existing native
route_with_radius/terminal selection and posted Reachability facts to determine
whether an EXISTING supported native operation was incorrectly represented by
the shim, a native preservation regression occurred, or the imported Tanner
caller accepts an ordinary legal arrival and then assumes too short a timeout.

Required outcome: cite the actual existing contract and a falsifier for each
remaining hypothesis. Do not infer unobstructed adjacency from coordinate range
or guarantee indoor approach without evidence. Determine whether the proven
3274,3188 terminal is connected/reachable to requested3277,3191 under actual
selected local collision at the observed time; the final failure snapshot on
the later bank leg cannot answer that. If this fact is absent, identify the
minimum read-only diagnostic snapshot needed at the exact arrival/Trade point.
No worker may launch it; root owns controlled LIVE. Distinguish loaded-scene
reachability from packed route facts and missing publication from illegal
arrival. Avoid exhaustive new harness work for a missing small observation.

No Tanner-specific WalkNear tightening, generic radius change, delayed
Npc.interact completion, host timer expansion or forced NPC relocation is
authorized by this audit. If evidence establishes a real bridge defect, scope
a minimal mapping onto already-owned native capability while preserving host
semantics. If it establishes a foreign caller defect, provide exact source/
evidence and the supported-options impact for the operator's existing dim
policy. If unresolved, state that and the single next observation; do not issue
a categorical no-dim/native-fix verdict based only on timeout duration.

Root is independently building28d97f38 for reviewed resource/Mule work; those
binaries and active workers are unrelated to this read-only audit. Complete
with report/evidence paths and actual source identities; no routine review hop.
