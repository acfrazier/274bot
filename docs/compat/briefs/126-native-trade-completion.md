# Complete required native trade operations

Use sol defaults after fire capability card (serial runtime chain). Read AGENTS,
execution, fail-closed-dispatch,04-combat-production-design trade section and
paired3ba51755 FAIL receipts. Current trade.js is inherited but inadequate:
request queues player label Trade and returns undefined; offer ignores n/pick
and sends generic if-button; offerAll/removeAll loop generic component buttons.
Frozen API contract in src/bot/api/trade/Trade.ts requires request boolean,
selected item id+slot, offerAll with optional pick, bounded offer(n) count-dialog
and fresh observed settlement, removeAll() without required name, accept/decline.
Implement the smallest native Rust capabilities used by enabled paired callers.
Do not copy foreign controller bodies or trade negotiation/market policy.

Verify actual selected274/289 player menu operation and component item operations.
Keep global native action label matching exact; fix the trade adapter's mapping
rather than broadening labels. Use existing native Input/inventory operations,
count-dialog answer, posted trade facts and protocol seam—no second packet table.
Native Rust owns offer count sequence and removal progress/deadline; JS may run
user pick callbacks as projection, marshal arguments and dispatch native results.
Preserve exact slot/id, non-noted selection, requested amount never over-offered,
partner identity, offer-versus-confirm buttons, false/refusal conventions and
existing3000ms dialog/4000ms settlement bounds. Posted queue is not transfer proof.
Pause freezes, Stop/logout/restart invalidate; stale screens/generations/partners
must not be acted on. Missing capabilities fail closed, no fake success.

Own narrow api/script/host-play code and meaningful tests, report09b-native-trade.md
and evidence/native-trade/. No scenario/catalog fixture changes, client/engine,
ledger/STATE, remotes or LIVE. Root separately owns correcting the Mule bootstrap
fixture, which currently seeds no first essence/runes and may deadlock independently.
Do not fix this by altering foreign script policy. No new crafting/muling scheduler.

Run focused api/script/host-play tests and strictClippy from exact source export
using one exclusive reusable cache. Composed tests must reject wrong id/slot,
noted selection, over-offer, stale/missing screen, no progress and late completion;
verify exact native sends/results and cancellation. Commit scoped changes and
request SAME-card reviewer, then stop. Root runs actual two-client transfer proof
on both revisions/catalogs once native and fixture reviews are accepted.


Current ownership clarification (2026-09-11): audit133 and correction134 forbid
bulk snapshot roundtrips for native polling. Reuse the compact Rust-owned
observation seam introduced by134 or borrow existing host observations; do not
send complete loc/NPC/inventory/bank/widget arrays V8-to-JSON-to-Rust on each
poll, retain a world clone, or move candidate selection back into JavaScript.
JS sends caller arguments/callback projections and dispatches returned verbs.
Preserve delta/reset/session/hold behavior and native deadlines. This applies
to the new capability's implementation and same-card review. Exact frozen
source with one exclusive reusable Cargo cache is permitted by current
execution policy; no shared-target or empty-started claim if reused.
