# Preserve bounded reachability with native flood metadata

Branch codex/rs2b0t-multirevision. Read AGENTS/docs/execution and fail-closed-dispatch skill. Authorized Rust-owned reusable compatibility capability; do not copy foreign BFS/controller or add JS routing. After137 and audit138, own needed api query/flood, snapshot transport/encoding, host-play snapshot projection, script reachability binding, focused tests. Do not edit catalog/scenario fixtures or frozen scripts. Report04aj-bounded-native-reachability.md and evidence/bounded-native-reachability/. Same-card reviewer; no LIVE.

Read report09c-bounded-reachability-audit.md. Root2d589 Flax picked/deposited but original GoToFieldTask was suppressed by unbounded flood semantics. Original coordinate Reachability.canReach defaults maxSteps400; explicit caps64..20000 are expansion budgets. Current shim ignores budget, including a test asserting that omission. Existing Rust SceneQuery.can_reach already defines bounded expansion semantics. Complete that API mapping through a reusable compact projection built alongside existing flood, not per-JS-query BFS, deep snapshot copies or foreign task special cases. Preserve all native navigation clocks/routers and other view semantics.

Implementation judgment: store enough native earliest-dequeue metadata to answer exact and adjacent reach in O(1) without losing wall constraints. Adjacent result must include exact target reach as native can_reach does, and may use blocked targets through a valid orthogonal ingress. A whole-flood reachable_adj bit plus arbitrary neighbor rank is insufficient: the valid wall-respecting neighbor must fit budget. Use earliest valid adjacent/exact rank computed natively, or a similarly bounded correct representation; audit21KiB single-index estimate is incomplete if additional adjacency data is required, report actual sizes honestly. Reuse the single native flood; no second per-target search or retained wholeworld. Native expansion/enqueue order and off-by-one must be checked against existing SceneQuery.can_reach, not handwaved from geometric distance. Large budgets are scene-bounded; invalid/missing/unavailable data fail closed. Omitted budget400; explicit0 meaningful (origin exact or legitimate origin-adjacent). Keep sync reads.

Apply budget uniformly to coordinate queries, including tiles occupied by npc/ground. Do not let current entityOnTile fast path silently bypass explicit budgets; use the same native coordinate metadata rather than keep a known unbounded semantic exception. Entity query convenience booleans can remain what they are; the Reachability.canReach contract is the scoped fix. Existing isolate canStep/walkable and native navigation controller behavior remain unchanged.

Full/delta and logout/scene-unavailable/region/plane/isolate-replacement reset must clear metadata with reach availability. Do not let missing rank arrays produce true from stale bits. Extend protocol additively with consistent field presence; record extra per-view/bot bytes and avoid allocations on unchanged polls. No performance/savings claim without matched measurements.

Tests must compare bounded projected answers to existing Rust can_reach over meaningful open-grid/corridor/collision pocket/wall-blocked adjacency and zero/default/explicit/larger-than-scene budgets, including exact target versus adjacent earlier dequeue. Verify full/delta/clear/reset and npc/ground colocated query do not bypass caps. Update maxStepsIgnored test to true semantic regression. Use existing focused crate tests/features and affected integration independently; exact source export with an exclusively owned reusable cache acceptable and honestly labeled. Do not overwrite concurrent source/targets. No allbranch acceptance or live pass from static proof. Root reruns Flax274/289 both catalogs and relevant Brimhaven/reach-dependent cells after review.
