# Correct native Baker stall selection and the bounded cake adapter

Use grok46 defaults after named-bank t_bced5c76 SAME-card review. Root inserts
this slice before special to serialize shared shim/mod.rs. Own cake_stall.js,
cake_stall_data.js, api/content.rs (or a unique native cake module plus lib
export if needed), shim/mod.rs content composition, unique focused tests,
 docs/compat/04w-cake-stall-native-mapping.md and unique evidence/cake-stall-native-mapping/.
No load.rs/slot.rs/host-play/nav/client/fixtures/foreign edits, no LIVE. If a
required native registration would touch load.rs, report the exact seam and
root will coordinate; do not silently collide with another runtime owner.

Read04u-ardy-cake-native-audit.md and its immutable source/log identities.
All six Ardy failures first hit our incomplete CakeStall mapping; Guard/Knight
cannot reach pickpocket because RestockCakes wins. Correct this bounded native
mapping without recreating the foreign90s controller, stand-swap/chat policy,
self-lockout planner or a JavaScript task-loop. No dim, timeout expansion,
seeded progress, packet change or change to the imported script.

1. Publish host-owned selected Baker stall/stand/flee metadata through existing
content_json seam, following existing PICKPOCKET_SPOTS ownership. Verify
actual selected274/289 engine/cache/nav facts for stall loc2561 at2667,3310,
walkable stand2668,3312, alternate2669,3310 and flee2655,3298; do not simply
copy foreign TypeScript coordinates. Do not invent facts for unknown profiles.
Use posted native facts in cake_stall_data instead of current2661,3301 aliases.
Preserve honest unavailable-data behavior. No foreign world table or native
pack-format/routing-policy changes.
2. Filter matching Baker's stall name AND operation AND selected native target
before nearest selection. Preserve exact selected id/tile/op into existing
Loc.interact. A closer door/wrong stall must never prevent selection of the
qualifying stall or become the dispatched target. Reject depleted/wrong-op
rows and the unrelated second stall for this pinned case.
3. Map a bounded attempt onto existing native traversal/interact and observed
inventory/combat facts, honoring the caller's abort/shouldEat/fillTo and
external lockedOutUntil guard. Do not dispatch during caller lockout, after
abort, when full, or when already stocked. Revalidate after an awaited walk.
Only observed food gain can invoke onSteal; never invoke onReset without a
native reset action. Preserve callbacks as glue and no fabricated completion.
Keep any new decision or capability policy in Rust; JavaScript is thin
argument/callback/await mapping. No new JS retry/controller loop.

Root qualification on04u: its suggested one-attempt adapter is a bounded
compatibility slice, NOT proof of full90s CakeStall controller equivalence.
Do not return stocked merely because an action queued or one food arrived
below fillTo. If no valid existing native result can express partial progress,
preserve an honest non-stocked result and document the precise limitation;
do not invent a successful return contract. classifySteal stays an explicit
unsupported ancillary helper; this mapping must not call it or copy its policy.
needsCakeRestock must honor both capacity and requested count (full unrelated
pack cannot require more cake). Any observation wait uses established native
or existing mapping bounds; do not widen catalog/host timeouts to pass tests.

Meaningful tests: noise nearest plus farther qualifying stall still selects
correct id; no matching/depleted/wrong-op/other-stall produces no loc command;
posted both-revision native pins; absent selected facts fail honestly;
abort/combat/full/fillTo/caller-lockout gates; revalidation after await; actual
food delta calls onSteal once while mere admission does not; true stocked only
at goal; no invented resets; correct cake/bread/slice inventory scope. Avoid
source-string snapshot assertions. Preserve all unrelated tests/owners.

Exact host/client Git export plus only owned overlay, exclusively empty target
for first checks, focused tests and strict Clippy. Preserve failed attempts and
record any reuse honestly. Check disk before compile; root handles cleanup.
Commit scoped files and request SAME-card reviewer with exact source/checks,
return-contract limits and no LIVE claim, then STOP. Root repeats six failed
Ardy families and newer counterparts plus a native run under unchanged gold
bounds, requiring actual steal/XP, bank deposit, closed return and further work.
